\documentclass[../../../../main.tex]{subfiles}
% \documentclass[a4paper,10pt]{ltjsarticle}
\begin{document}
\rhead{京都大学後期$1994$年$3$番}
\setcounter{equation}{0}
\begin{oframed}
  \input{../../../problems/1994/3/problem.tex}
\end{oframed}
\setlength{\columnseprule}{0.4pt}
\begin{multicols}{2}
  {\bf[解]}

  \begin{figure}[H]
    \centering
    \begin{tikzpicture}
      \begin{axis}[
          xmin=-0.5, xmax=5,
          ymin=-0.5, ymax=4,
          axis lines=middle, % Draw axes through the origin
          xlabel=$x$,
          ylabel=$y$,
          xtick={1,4},
          ytick={1,3},
          grid=none, % No background grid
          xticklabels={$1$,$X$}, % Only explicitly label 0 and 1 on the x-axis
          yticklabels={$1$,$Y$}, % Only explicitly label 0 and 1 on the y-axis
          clip=false % Allow drawing elements outside the strict axis boundaries
        ]

        % O(0,0) label
        \node[below left] at (axis cs:0,0) {O};

        % Circle equation: (x-1)^2+(y-1)^2=1
        % Center at (1,1), Radius 1
        \draw (axis cs:1,1) circle (1);

        % Define the center of the circle for calculations
        \coordinate (C) at (axis cs:1,1);
        \fill (C) circle (1.5pt); % Draw a dot for the center

        % Define the tangent line PQ based on the previous problem's solution (x/4 + y/3 = 1)
        \coordinate (P) at (axis cs:4,0);
        \coordinate (Q) at (axis cs:0,3);
        \draw (P) -- (Q); % Draw the line segment PQ
        \node[above right] at (P) {P}; % Label P
        \node[above left] at (Q) {Q}; % Label Q

        % Points A and B on the circle (tangent points or diametrically opposite)
        % These coordinates are derived from the problem's solution (B is (8/5, 9/5), A is (2/5, 1/5))
        \coordinate (A) at (axis cs:0.4,0.2); % A is (0.4, 0.2)
        \coordinate (B) at (axis cs:1.6,1.8); % B is (1.6, 1.8)
        % \fill (A) circle (1.5pt); % Draw dot for A
        \fill (B) circle (1.5pt); % Draw dot for B
        \fill (P) circle (1.5pt); % Draw dot for B
        \fill (Q) circle (1.5pt); % Draw dot for B

        % \node[below] at (A) {A}; % Label A
        \node[above] at (B) {B}; % Label B

        % Draw the line segment AB, which passes through the center C (diameter)
        \draw (1,1) -- (B);
        % Label for the line L (as seen in the original image near the center)
        \node[above left] at (axis cs:1.2,1.2) {L};
        \draw[dashed] (1,0) -- (1,1) -- (0,1);

      \end{axis}
    \end{tikzpicture}
    \caption{円Cと点PRの様子}
    \label{1994-3:fig:1}
  \end{figure}

  \begin{figure}[H]
    \centering
    \begin{tikzpicture}
      \begin{axis}[
          xmin=-0.5, xmax=4.5,
          ymin=-0.5, ymax=3.5,
          axis lines=middle, % Draw axes through the origin
          xlabel=$x$,
          ylabel=$y$,
          xtick={1,1/2},
          ytick={1,2/3},
          grid=none, % No background grid
          xticklabels={$1$,$X$}, % Only explicitly label 0 and 1 on the x-axis
          yticklabels={$1$,$Y$}, % Only explicitly label 0 and 1 on the y-axis
          clip=false % Allow drawing elements outside the strict axis boundaries
        ]

        % O(0,0) label
        \node[below left] at (axis cs:0,0) {O};

        % Circle equation: (x-1)^2+(y-1)^2=1
        % Center at (1,1), Radius 1
        \draw (axis cs:1,1) circle (1);

        % Define the center of the circle for calculations
        \coordinate (C) at (axis cs:1,1);
        \fill (C) circle (1.5pt); % Draw a dot for the center

        % Define the tangent line PQ based on the previous problem's solution (x/4 + y/3 = 1)
        \coordinate (P) at (axis cs:1/2,0);
        \coordinate (Q) at (axis cs:0,2/3);
        \draw (P) -- (Q); % Draw the line segment PQ
        \node[above right] at (P) {P}; % Label P
        \node[above right] at (Q) {Q}; % Label Q

        % Points A and B on the circle (tangent points or diametrically opposite)
        % These coordinates are derived from the problem's solution (B is (8/5, 9/5), A is (2/5, 1/5))
        \coordinate (A) at (axis cs:0.4,0.2); % A is (0.4, 0.2)
        \coordinate (B) at (axis cs:1.6,1.8); % B is (1.6, 1.8)
        % \fill (A) circle (1.5pt); % Draw dot for A
        \fill (B) circle (1.5pt); % Draw dot for B
        \fill (P) circle (1.5pt); % Draw dot for B
        \fill (Q) circle (1.5pt); % Draw dot for B

        % \node[below] at (A) {A}; % Label A
        \node[above] at (B) {B}; % Label B

        % Draw the line segment AB, which passes through the center C (diameter)
        \draw (1,1) -- (B);
        % Label for the line L (as seen in the original image near the center)
        \node[above left] at (axis cs:1.2,1.2) {L};
        \draw[dashed] (1,0) -- (1,1) -- (0,1);

      \end{axis}
    \end{tikzpicture}
    \caption{円Cと点PQの様子．線分PQが円の下側に来る場合．}
    \label{1994-3:fig:2}
  \end{figure}



  題意の円の方程式は
  \begin{align*}
    C:(x-1)^2+(y-1)^2=1
  \end{align*}
  である．
  また，題意の点PとQの座標を，$0<X,Y$として
  P$(X,0)$ Q$(0,Y)$とおく.
  すると，直線PQの方程式は
  \begin{align*}
    \frac{x}{X} + \frac{y}{Y} = 1
  \end{align*}
  である．
  この直線PQが円Cの接線である時，直線PQとCの中心$(1,1)$の距離が$1$だから，
  点と直線の距離公式より
  \begin{align*}
    \frac{|X+Y-XY|}{\sqrt{X^2+Y^2}} = 1
  \end{align*}
  両辺$0$以上だから$2$乗して
  \begin{align*}
     & (X+Y-XY)^2 = X^2+Y^2                     \\
    \therefore
     & X^2+Y^2+X^2Y^2+2XY-2X^2Y-2XY^2 = X^2+Y^2 \\
    \therefore
     & X^2Y^2+2XY-2X^2Y-2XY^2 = 0
  \end{align*}
  $XY \ne 0$だから両辺$XY$で割ってよく，
  \begin{align}
    XY+2-2X-2Y=0 \label{1994-3:eq:1}
  \end{align}
  を得る．

  \begin{figure}[H]
    \centering
    \begin{tikzpicture}
      \begin{axis}[
          xmin=-0.5, xmax=5,
          ymin=-0.5, ymax=4,
          axis lines=middle, % Draw axes through the origin
          xlabel=$x$,
          ylabel=$y$,
          xtick={1,4},
          ytick={1,3},
          grid=none, % No background grid
          xticklabels={$1$,$X$}, % Only explicitly label 0 and 1 on the x-axis
          yticklabels={$1$,$Y$}, % Only explicitly label 0 and 1 on the y-axis
          clip=false % Allow drawing elements outside the strict axis boundaries
        ]

        % O(0,0) label
        \node[below left] at (axis cs:0,0) {O};

        % Circle equation: (x-1)^2+(y-1)^2=1
        % Center at (1,1), Radius 1
        \draw (axis cs:1,1) circle (1);

        % Define the center of the circle for calculations
        \coordinate (C) at (axis cs:1,1);
        \fill (C) circle (1.5pt); % Draw a dot for the center

        % Define the tangent line PQ based on the previous problem's solution (x/4 + y/3 = 1)
        \coordinate (P) at (axis cs:4,0);
        \coordinate (Q) at (axis cs:0,3);
        \coordinate (R) at (axis cs:{(4+3*sqrt(3))/2},{(3+4*sqrt(3))/2});
        \coordinate (Rprime) at (axis cs:{(4-3*sqrt(3))/2},{(3-4*sqrt(3))/2});
        \draw (P) -- (Q) -- (R) -- cycle; % Draw the line segment PQ
        \draw[dashed] (P) -- (Rprime) -- (Q); % Draw the line segment PQ
        \node[above right] at (P) {P}; % Label P
        \node[above left] at (Q) {Q}; % Label Q
        \node[above left] at (R) {R}; % Label R


        % Points A and B on the circle (tangent points or diametrically opposite)
        % These coordinates are derived from the problem's solution (B is (8/5, 9/5), A is (2/5, 1/5))
        \coordinate (A) at (axis cs:0.4,0.2); % A is (0.4, 0.2)
        \coordinate (B) at (axis cs:1.6,1.8); % B is (1.6, 1.8)
        % \fill (A) circle (1.5pt); % Draw dot for A
        \fill (B) circle (1.5pt); % Draw dot for B
        \fill (P) circle (1.5pt); % Draw dot for B
        \fill (Q) circle (1.5pt); % Draw dot for B
        \fill (R) circle (1.5pt); % Draw dot for B

        % \node[below] at (A) {A}; % Label A
        \node[above] at (B) {B}; % Label B

        % Draw the line segment AB, which passes through the center C (diameter)
        \draw (1,1) -- (B);
        % Label for the line L (as seen in the original image near the center)
        \node[above left] at (axis cs:1.2,1.2) {L};
        \draw[dashed] (1,0) -- (1,1) -- (0,1);
      \end{axis}
    \end{tikzpicture}
    \caption{円Cと三角形PQRの様子．点Rが線分PQの上側にある場合のみ，Rが第一象限に存在する．}
    \label{1994-3:fig:3}
  \end{figure}

  次に，点Rの座標を$X,Y$を用いて表す．
  線分PQの中点をMとすると，ベクトルMRはベクトルPQと直交して，長さは
  \begin{align*}
    MR = \frac{\sqrt{3}}{2}PQ
  \end{align*}
  を満たすから，ベクトルPQの方向ベクトルが$(X,-Y)$と書けることより
  \begin{align*}
    \vec{OR}
     & = \vec{OM} + \vec{MR} \\
     & = \frac{1}{2}\mqty(X  \\Y) \pm \frac{\sqrt{3}}{2}\mqty(Y \\ X) \\
  \end{align*}
  と書ける．従って，題意より点Rの座標が$(a,b)$であることから
  \begin{align}
    \begin{dcases}
      a = \frac{1}{2}(X \pm \sqrt{3}Y) \\
      b = \frac{1}{2}(Y \pm \sqrt{3}X)
    \end{dcases} \quad \text{(複号同順)} \label{1994-3:eq:2}
  \end{align}
  となる．
  ここで，題意より点Rが第一象限にあることより$a,b>0$だから
  \begin{align*}
     & \begin{dcases}
         X \pm \sqrt{3}Y > 0 \\
         Y \pm \sqrt{3}X > 0
       \end{dcases} \\
    \therefore
     & \begin{dcases}
         X > \mp \sqrt{3}Y \\
         Y > \mp \sqrt{3}X
       \end{dcases}
  \end{align*}
  であるが，複号負の時，
  \begin{align*}
     & Y > \sqrt{3}X > \sqrt{3}\cdot \sqrt{3}Y \\
    \therefore
     & Y > 3Y
  \end{align*}
  となるが，これを満たす$X,Y>0$は存在しないから，
  取りうるのは複号正のみである．
  従って\cref{1994-3:eq:2}で複号正をとって
  \begin{align}
    \begin{dcases}
      a = \frac{1}{2}(X + \sqrt{3}Y) \\
      b = \frac{1}{2}(Y + \sqrt{3}X)
    \end{dcases} \label{1994-3:eq:3}
  \end{align}
  となる．逆にこの時，$X,Y>0$から自動的に$a,b>0$も満たされる．

  よって，以下\cref{1994-3:eq:1}および$X,Y>0$のもとで\cref{1994-3:eq:3}の満たす関係を調べれば良い．
  \cref{1994-3:eq:3}を逆に解いて
  \begin{align*}
    \begin{dcases}
      X = -a+\sqrt{3}b \\
      Y = \sqrt{3}a-b
    \end{dcases}
  \end{align*}
  だから，これを\cref{1994-3:eq:1}および$X,Y>0$に代入して
  \begin{align*}
     & \begin{dcases}
         \left(-a+\sqrt{3}b\right)\left(\sqrt{3}a-b\right) +2-2\left(-a+\sqrt{3}b+\sqrt{3}a-b\right)=0 \\
         -a+\sqrt{3}b > 0                                                                              \\
         \sqrt{3}a-b > 0
       \end{dcases} \\
    \therefore
     & \begin{dcases}
         \sqrt{3}(a^2+b^2)-4ab-2+2(1-\sqrt{3})(a-b) = 0 \\
         \sqrt{3}b > a                                  \\
         \sqrt{3}a > b
       \end{dcases}
  \end{align*}
  が求める関係式である．$\cdots$(答)

  \vspace{10pt}
  {\bf[解説]}

  % TODO ::   Y = 2(1+X)/(X-2)の図示
  % TODO :: 解の領域の図示

  \newpage
\end{multicols}
\end{document}