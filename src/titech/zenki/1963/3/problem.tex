\documentclass[../../../../main.tex]{subfiles}
\begin{document}

図のように，1辺の長さ2の正方形 $ABCD$ の内部に，
対角線の交点 $O$ を中心とする半径 $r$ の円をえがき，
$AB$，$AD$ に平行なその接線の交点を $E$ とする。
いま，$E$ を中心とする円を，円 $O$ との相似の中心が $A$ であるようにえがく。
このとき，次の問に答えよ。

\begin{figure}[htbp]
  \centering
  \begin{tikzpicture}[scale=1.8]
    \coordinate (A) at (-1,-1);
    \coordinate (B) at (1,-1);
    \coordinate (C) at (1,1);
    \coordinate (D) at (-1,1);
    \coordinate (O) at (0,0);

    \draw[thick] (A) -- (B) -- (C) -- (D) -- cycle;
    \draw (A) -- (C);
    \draw (B) -- (D);

    \def\r{0.45}
    \draw[thick] (O) circle (\r);

    \coordinate (E) at (-\r, -\r);
    \node[below left] at (E) {$E$};

    \draw (E) -- (-\r, 0);
    \draw (E) -- (0, -\r);

    \node[below left] at (A) {$A$};
    \node[below right] at (B) {$B$};
    \node[above right] at (C) {$C$};
    \node[above left] at (D) {$D$};
    \node[above right] at (O) {$O$};
  \end{tikzpicture}
\end{figure}

\begin{enumerate}
  \item 2円（円 $O$ と円 $E$）が交わるような $r$ の値の範囲を求めよ。
  \item 2円が交わるとき，交点を $K$，$L$ とすれば，$\theta=\angle KEL$ はどんな範囲にあるか。
\end{enumerate}

\end{document}
