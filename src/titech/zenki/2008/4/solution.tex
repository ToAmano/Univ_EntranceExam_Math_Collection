\documentclass[../../main.tex]{subfiles}
\begin{document}

\begin{flushright}
\footnotesize\textit{【自動文字起こし・要確認】}
\end{flushright}

{\bf [解]}
$\alpha$を固定し，$L_1:y=-\tan\alpha\cdot x,\ L_2:y=\tan\alpha\cdot x$（$x\ge0$）とする．以下$C=\cos\alpha,\ S=\sin\alpha$と書く．$P,Q$の位置を表す媒介変数$a,b>0$を用いて
\begin{align*}
\overrightarrow{OP}=a\begin{pmatrix}C\\-S\end{pmatrix},\qquad\overrightarrow{OQ}=b\begin{pmatrix}C\\S\end{pmatrix}
\end{align*}
とおける．$\angle POQ=2\alpha$なので，$\triangle OPQ$に余弦定理を用いて（$PQ=1$より）
\begin{align*}
1=a^2+b^2-2ab\cos2\alpha.\tag{①}
\end{align*}

\begin{figure}[htb]
\centering
\begin{tikzpicture}[scale=2.6]
  \def\al{25}
  \draw[->] (-0.3,0) -- (1.5,0) node[right]{$x$};
  \draw[thick,gray] (0,0) -- ({1.3*cos(\al)},{-1.3*sin(\al)}) node[right]{$L_1$};
  \draw[thick,gray] (0,0) -- ({1.3*cos(\al)},{1.3*sin(\al)}) node[right]{$L_2$};
  \coordinate (O) at (0,0);
  \coordinate (P) at ({0.8*cos(\al)},{-0.8*sin(\al)});
  \coordinate (Q) at ({0.55*cos(\al)},{0.55*sin(\al)});
  \coordinate (M) at ($(P)!0.5!(Q)$);
  \coordinate (R) at ($(M)!{sqrt(3)}!-90:(Q)$);
  \draw[thick] (P) -- (Q) -- (R) -- cycle;
  \filldraw (O) circle(0.012) node[left]{$O$};
  \filldraw (P) circle(0.012) node[below]{$P$};
  \filldraw (Q) circle(0.012) node[above]{$Q$};
  \filldraw (R) circle(0.012) node[right]{$R$};
  \draw (0.25,0) arc (0:\al:0.25) node[midway,right]{\scriptsize$\alpha$};
  \draw (0.25,0) arc (0:-\al:0.25);
\end{tikzpicture}
\caption{$L_1,L_2$上の$P,Q$と正三角形$PQR$（模式図，$R$は実際には$\triangle PQR$が正三角形になる位置）}
\end{figure}

$M$を$PQ$の中点とすると，$\overrightarrow{PQ}=\bigl((b-a)C,\,(b+a)S\bigr)$だから，$\overrightarrow{PQ}\perp\overrightarrow{MR}$，$|\overrightarrow{MR}|=\dfrac{\sqrt{3}}{2}|\overrightarrow{PQ}|=\dfrac{\sqrt{3}}{2}$．$R$が直線$PQ$に関して$O$と反対側にあることから
\begin{align*}
\overrightarrow{MR}=\dfrac{\sqrt{3}}{2}\begin{pmatrix}(b+a)S\\-(b-a)C\end{pmatrix}.
\end{align*}
$M=\Bigl(\dfrac{a+b}{2}C,\ \dfrac{b-a}{2}S\Bigr)$なので，$I=a+b,\ H=b-a$とおくと
\begin{align*}
\overrightarrow{OR}=\overrightarrow{OM}+\overrightarrow{MR}=\dfrac{1}{2}\begin{pmatrix}IC\\HS\end{pmatrix}+\dfrac{\sqrt{3}}{2}\begin{pmatrix}IS\\-HC\end{pmatrix}=\dfrac{1}{2}\begin{pmatrix}I(C+\sqrt{3}S)\\H(S-\sqrt{3}C)\end{pmatrix}.
\end{align*}
よって$R=(X,Y)$とおくと
\begin{align*}
X=\dfrac{I}{2}(C+\sqrt{3}S),\qquad Y=\dfrac{H}{2}(S-\sqrt{3}C).\tag{②}
\end{align*}

\textbf{(1)} $PQ\parallel y$軸のとき，$\overrightarrow{PQ}$の$x$成分$(b-a)C=0$かつ$C\neq0$（$0<\alpha<\dfrac{\pi}{3}$）だから$a=b$，すなわち$H=0$．①に代入して$1=2a^2(1-\cos2\alpha)=4a^2S^2$より$a=\dfrac{1}{2S}$，$I=2a=\dfrac{1}{S}$．②より
\begin{align*}
X=\dfrac{1}{2S}(C+\sqrt{3}S)=\dfrac{1}{2\tan\alpha}+\dfrac{\sqrt{3}}{2},\qquad Y=0.
\end{align*}
よって$R=\Bigl(\dfrac{1}{2\tan\alpha}+\dfrac{\sqrt{3}}{2},\ 0\Bigr)$．

\textbf{(2)} $C+\sqrt{3}S=2\sin\bigl(\alpha+\dfrac{\pi}{6}\bigr)$，$S-\sqrt{3}C=2\sin\bigl(\alpha-\dfrac{\pi}{3}\bigr)$である．$0<\alpha<\dfrac{\pi}{3}$のとき$\alpha+\dfrac{\pi}{6}\in\bigl(\dfrac{\pi}{6},\dfrac{\pi}{2}\bigr)$，$\alpha-\dfrac{\pi}{3}\in\bigl(-\dfrac{\pi}{3},0\bigr)$だから，これらはいずれも$0$でない（③）．よって②から
\begin{align*}
I=\dfrac{2X}{C+\sqrt{3}S},\qquad H=\dfrac{2Y}{S-\sqrt{3}C}.\tag{②$'$}
\end{align*}
一方，$a=\dfrac{I-H}{2},\ b=\dfrac{I+H}{2}$より$a^2+b^2=\dfrac{I^2+H^2}{2}$，$ab=\dfrac{I^2-H^2}{4}$だから，①は
\begin{align*}
1=\dfrac{I^2+H^2}{2}-\dfrac{I^2-H^2}{2}\cos2\alpha\quad(\because②')
\end{align*}
すなわち$2=I^2(1-\cos2\alpha)+H^2(1+\cos2\alpha)=2S^2I^2+2C^2H^2$．②$'$を代入して整理すると
\begin{align*}
1=\dfrac{X^2}{\left(\dfrac{C+\sqrt{3}S}{2S}\right)^2}+\dfrac{Y^2}{\left(\dfrac{S-\sqrt{3}C}{2C}\right)^2}.
\end{align*}
これは（$\alpha$を固定するとき）$X,Y$に関する楕円の方程式だから，$P,Q$が$PQ=1$を保ちながら動くときの点$R(X,Y)$の軌跡は，この楕円の一部である．


\end{document}
