\documentclass[../../main.tex]{subfiles}
\begin{document}

\begin{flushright}
\footnotesize\textit{【自動文字起こし・要確認】}
\end{flushright}

{\bf [解]}
長方形$R$の4頂点を，$X(0,a)$，$Y(b,a)$，$Z(b,0)$，$W(0,0)$とする（$XY=b$，$XW=a$，$0<a<b<2a$）．円$A$は性質(P)から2辺$XY,XW$に接するとしてよく，半径を$x$とすると中心は$(x,a-x)$であり，
\begin{align}
0<x<\dfrac{a}{2} \quad\cdots\text{(＊)}
\end{align}
が必要である．

\textbf{(1)} 円$A$に外接する性質(P)の円は，対称性から次の4つで尽くされる：頂点$W$で2辺$XW,WZ$に接する円$C_1$（半径$r_1$，中心$(r_1,r_1)$），頂点$Z$で2辺$WZ,ZY$に接する円$C_2$（半径$r_2$，中心$(b-r_2,r_2)$），頂点$Y$で2辺$ZY,YX$に接する円$C_3$（半径$r_3$，中心$(b-r_3,a-r_3)$），そして頂点$X$で円$A$と同じ2辺$XY,XW$に接し，$A$より角$X$寄りにある円$C_4$（半径$r_4$，中心$(r_4,a-r_4)$）．

\subparagraph{$C_1$について：} $A,C_1$が外接する条件から
\begin{align}
(x-r_1)^2+(a-x-r_1)^2=(x+r_1)^2
\end{align}
整理すると（$a-x-r_1>0$に注意して）
\begin{align}
\begin{split}
(a-x-r_1)^2&=4xr_1 \quad\therefore\ a-x-r_1=2\sqrt{xr_1} \\
a&=x+r_1+2\sqrt{xr_1}=(\sqrt{x}+\sqrt{r_1})^2 \quad\therefore\ r_1=(\sqrt{a}-\sqrt{x})^2
\end{split}
\end{align}

\subparagraph{$C_3$について：} 同様に
\begin{align}
\begin{split}
(b-x-r_3)^2+(x-r_3)^2&=(x+r_3)^2 \ \Longrightarrow\ b=x+r_3+2\sqrt{xr_3}=(\sqrt{x}+\sqrt{r_3})^2 \\
&\therefore\ r_3=(\sqrt{b}-\sqrt{x})^2
\end{split}
\end{align}

\subparagraph{$C_2$について：} $A,C_2$が外接する条件から
\begin{align}
(b-x-r_2)^2+(a-x-r_2)^2=(x+r_2)^2
\end{align}
展開して整理すると
\begin{align}
(x+r_2)^2-2(a+b)(x+r_2)+a^2+b^2=0
\end{align}
$u=x+r_2$とおくと$u^2-2(a+b)u+(a^2+b^2)=0$より
\begin{align}
u=(a+b)\pm\sqrt{2ab}
\end{align}
複号のうち$+$は$r_2$が大きくなりすぎ図形的制約に反するので不適，
\begin{align}
r_2=(a+b)-\sqrt{2ab}-x
\end{align}

\subparagraph{$C_4$について：} $A,C_4$は同じ角$X$の二等分線上に中心があり，外接条件から中心間距離$\sqrt{2}(x-r_4)=x+r_4$（$r_4<x$）．よって
\begin{align}
\sqrt{2}\,x-\sqrt{2}\,r_4=x+r_4 \quad\therefore\ r_4=\dfrac{(\sqrt{2}-1)x}{\sqrt{2}+1}=(3-2\sqrt{2})x
\end{align}

$r_1=(\sqrt{a}-\sqrt{x})^2$が意味を持ち，かつ$0<r_1<a/2$をみたすことなどから，$0<r_k<a/2$（$k=1,2,3$）の存在条件を調べればよい．$r_1<a/2$の条件は$\sqrt{a}-\sqrt{x}<\sqrt{a/2}$，$r_3<a/2$の条件は$\sqrt{b}-\sqrt{x}<\sqrt{a/2}$であり，$b>a$からこちらがより強い条件を与える：
\begin{align}
\sqrt{x}>\sqrt{b}-\sqrt{\dfrac{a}{2}} \quad\therefore\ x>\left(\sqrt{b}-\sqrt{\dfrac{a}{2}}\right)^2
\end{align}
（$r_2$を与える$u$の方程式は判別式$(a+b)^2-(a^2+b^2)=2ab>0$から常に解をもち，$r_2$は自動的に存在する．）以上と(＊)をあわせて，もとめる条件は
\begin{align}
\left(\sqrt{b}-\sqrt{\dfrac{a}{2}}\right)^2<x<\dfrac{a}{2}
\end{align}

\begin{figure}[htb]
\begin{center}
\begin{tikzpicture}[scale=0.55, >=stealth]
  % 長方形 R (a=4.6, b=7 として図示)
  \draw[thick] (0,0) rectangle (7,4.6);
  \node[below left] at (0,0) {$W$};
  \node[below right] at (7,0) {$Z$};
  \node[above right] at (7,4.6) {$Y$};
  \node[above left] at (0,4.6) {$X$};
  % 円A (中心(x,a-x), x=1.6) と外接する4円 C1,C2,C3,C4 (半径は本文の公式値)
  \draw (1.6,3.0) circle (1.6);
  \draw (0.275,4.325) circle (0.275);
  \draw (0.774,0.774) circle (0.774);
  \draw (5.025,1.975) circle (1.975);
  \draw (5.093,2.693) circle (1.907);
  \node at (1.6,3.0) {\small$A$};
  \node at (0.774,0.774) {\small$C_1$};
  \node at (5.025,1.975) {\small$C_2$};
  \node at (5.093,2.693) {\small$C_3$};
  \node[above] at (0.275,4.6) {\tiny$C_4$};
\end{tikzpicture}
\end{center}
\caption{長方形$WXYZ$と円$A$，$C_1$，$C_2$，$C_3$，$C_4$の配置}
\end{figure}

\textbf{(2)} (1)の条件下で，まず$C_4$の半径$r_4=(3-2\sqrt{2})x$は$3-2\sqrt{2}\approx0.17$と小さいため，常に$r_1$より小さい（実際$r_1=(\sqrt{a}-\sqrt{x})^2$は$x<a/2$の範囲で$r_4$より大きいことが確かめられる）．また
\begin{align}
\begin{split}
r_3-r_2&=(b+x-2\sqrt{bx})-\bigl((a+b)-\sqrt{2ab}-x\bigr)=2x-2\sqrt{bx}+\sqrt{2ab}-a \\
&=2\left(\sqrt{x}-\dfrac{\sqrt{2a}}{2}\right)\left(\sqrt{x}-\dfrac{2\sqrt{b}-\sqrt{2a}}{2}\right)
\end{split}
\end{align}
(1)の範囲内でこれが負であることが確かめられるので，$r_3<r_2$．以上から4円の半径の大小は
\begin{align}
r_2>r_3>r_1>r_4
\end{align}
となり，$A$に外接する4円のうち「2番目に大きい円」$B$は半径$r_3$の$C_3$である．

したがって$A$と$B$の面積の和$S$は
\begin{align}
S=\pi\left(x^2+r_3^2\right)=\pi\left\{x^2+(\sqrt{b}-\sqrt{x})^4\right\}
\end{align}
$X=\sqrt{x}$とおいて微分すると
\begin{align}
\begin{split}
\dfrac{dS}{dx}&=\pi\left\{2x+4(\sqrt{x}-\sqrt{b})^3\cdot\dfrac{1}{2\sqrt{x}}\right\}=\pi\left\{2X^2+\dfrac{2(X-\sqrt{b})^3}{X}\right\} \\
&=\dfrac{2\pi}{X}\left\{X^3+X^3-3\sqrt{b}X^2+3bX-b\sqrt{b}\right\}=\dfrac{4\pi}{X}\left(X-\dfrac{\sqrt{b}}{2}\right)\left(X^2-\sqrt{b}X+b\right) \\
&=\dfrac{4\pi}{X}\left(X-\dfrac{\sqrt{b}}{2}\right)\left\{\left(X-\dfrac{\sqrt{b}}{2}\right)^2+\dfrac{3b}{4}\right\}
\end{split}
\end{align}
$\{\ \cdots\ \}$の中は常に正だから，$dS/dx$の符号は$X-\sqrt{b}/2$の符号と一致し，(1)の範囲$\sqrt{b}-\sqrt{a/2}<X<\sqrt{a/2}$の中に$X=\sqrt{b}/2$が含まれる（$2a>b$より$\sqrt{b}/2<\sqrt{a/2}$）ので，下表を得る．
\begin{center}
\begin{tabular}{c|ccccc}
$X$ & $\sqrt{b}-\sqrt{a/2}$ & & $\sqrt{b}/2$ & & $\sqrt{a/2}$ \\
\hline
$S'$ & & $-$ & $0$ & $+$ & \\
\hline
$S$ & & $\searrow$ & & $\nearrow$ &
\end{tabular}
\end{center}
よって$X=\sqrt{b}/2$，すなわち$x=b/4$で$S$は最小となり，この時$r_3=(\sqrt{b}-\sqrt{b/4})^2=(\sqrt{b}/2)^2=b/4$だから
\begin{align}
\min S=\pi\left\{\left(\dfrac{b}{4}\right)^2+\left(\dfrac{b}{4}\right)^2\right\}=\dfrac{b^2}{8}\pi
\end{align}


\newpage

\end{document}
