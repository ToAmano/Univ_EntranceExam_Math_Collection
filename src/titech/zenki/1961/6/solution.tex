\documentclass[../../main.tex]{subfiles}
\usepackage{amsmath}	% required for `\align' (yatex added)
\begin{document}

{\bf [解]}
与えられた条件
\begin{align}
 -\dfrac{1}{2} < f'(x) < \dfrac{1}{2} \label{eq:1}
\end{align}
の両辺を不定積分して
\begin{align}
-\dfrac{1}{2}x + C_1 < f(x) < \dfrac{1}{2}x + C_2 \label{eq:2}
\end{align}
である．ただし$C_1$, $C_2$は積分定数である．

(1)
 $F(x) = f(x) - x$ とおくと\cref{eq:1}より$F'(x) = f'(x) - 1 < 0$ だから$F(x)$ は単調減少である．
 さらに，\cref{eq:2}の両辺から$x$を引くと
  \begin{align}
    -\dfrac{3}{2}x + C_1 < F(x) < -\dfrac{1}{2}x + C_2   
  \end{align}
 だからはさみうちの定理から，
\begin{align}
 &\lim_{x\to \infty}F(x) = -\infty \\
 &\lim_{x\to -\infty}F(x) = \infty 
\end{align}
となる．

$F(x)$ は連続関数だから以上の事実より $F(x) = 0$ はただ1つの実解を持つ．

(2)
題意の条件より $f(\alpha) = \alpha$ である．従って与えられた漸化式の両辺からこの式を引いて
  \begin{align}
    a_{n+1} - \alpha = f(a_n) - f(\alpha) \label{eq:3}
  \end{align}
と書ける．

以下場合分けして考える．
まず$a_n = \alpha$ なる $n$ がある時，\cref{eq:3}から$n \le m$ をみたす $m$ に対して，$a_m = \alpha$ だから，$a_m \to \alpha \ (n \to \infty)$ である．

その他の場合平均値の定理から，$f(a_n) - f(\alpha) = f'(c)(a_n - \alpha)$ をみたす $c$ がある．\cref{eq:1,eq:3}より
\begin{align}
 |a_{n+1} - \alpha| = |f'(c)||a_n - \alpha| < \dfrac{1}{2}|a_n - \alpha|
\end{align}
だからくり返し用いて，
\begin{align}
   |a_n - \alpha| < \left(\dfrac{1}{2}\right)^{n-1}|a_1 - \alpha| \xrightarrow{n \to \infty} 0
 \end{align}
を得るから，挟み撃ちの定理から
\begin{align}
   \lim_{n \to \infty} a_n \longrightarrow \alpha
\end{align}
である．

以上から題意は示された．

% {\bf [解説]}

% \begin{figure}[htb]
%  \begin{center}
%   \begin{tikzpicture}[x=1.8cm, y=1.8cm, >=stealth]
%     % 軸の描画
%     \draw[->, thick] (-0.5, 0) -- (3.5, 0) node[right] {$x$};
%     \draw[->, thick] (0, -0.5) -- (0, 3.2) node[above] {$y$};
%     \node[below left] at (0,0) {$O$};

%     % y = x の直線
%     \draw[gray!70, thick, dashed] (-0.3, -0.3) -- (3.2, 3.2) node[right, black] {$y = x$};

%     % 傾き +/- 1/2 の包囲線（参考線）
%     % y = 1/2 x + C2, y = -1/2 x + C1 の傾きの視覚化
%     \draw[dotted, gray!50] (0, 0.3) -- (3.5, 2.05) node[right, font=\tiny] {傾き $1/2$};

%     % f(x) = (1/3)*sin(x) + 1.2 の曲線描画 (|f'| <= 1/3 < 1/2)
%     % (rad オプションでラジアン計算)
%     \draw[very thick, blue!80!black, domain=-0.3:3.3, samples=100, smooth]
%         plot (\x, {1/3*sin(\x r) + 1.2}) node[right] {$y = f(x)$};

%     % 交点 alpha の座標計算
%     % (1/3)*sin(x) + 1.2 = x の解は x ≒ 1.439
%     \def\alphaVal{1.439}

%     % 交点 alpha のマーカーと垂線
%     \draw[dashed, red!80!black] (\alphaVal, 0) -- (\alphaVal, \alphaVal);
%     \fill[red!80!black] (\alphaVal, \alphaVal) circle (2pt);
%     \node[below, red!80!black, font=\small] at (\alphaVal, 0) {$\alpha$};

%     % -------------------------------------------------------------
%     % 漸化式 a_{n+1} = f(a_n) の軌跡 (Cobweb Plot)
%     % 初期値 a1 = 0.3 からスタート
%     % -------------------------------------------------------------
%     \def\aOne{0.3}
%     \def\aTwo{1.30}  % f(0.3) ≒ 1.298
%     \def\aThree{1.52} % f(1.298) ≒ 1.521
%     \def\aFour{1.42}  % f(1.521) ≒ 1.53 -> 1.42 付近へ収束

%     % a1 の位置
%     \node[below, font=\small] at (\aOne, 0) {$a_1$};
%     \draw[dashed, gray] (\aOne, 0) -- (\aOne, \aTwo);

%     % a1 -> f(a1)=a2 -> y=x -> f(a2)=a3 の階段描画
%     \draw[thick, purple, ->] (\aOne, \aTwo) -- (\aTwo, \aTwo) node[midway, above, font=\tiny] {};
%     \draw[thick, purple, ->] (\aTwo, \aTwo) -- (\aTwo, \aThree);
%     \draw[thick, purple, ->] (\aTwo, \aThree) -- (\aThree, \aThree);
%     \draw[thick, purple, ->] (\aThree, \aThree) -- (\aThree, \aFour);

%     % 各項のx軸上への投影 (a2, a3)
%     \draw[dashed, gray] (\aTwo, 0) -- (\aTwo, \aTwo);
%     \node[below, font=\small] at (\aTwo, 0) {$a_2$};
    
%     \draw[dashed, gray] (\aThree, 0) -- (\aThree, \aThree);
%     \node[below, font=\small] at (\aThree, 0) {$a_3$};

%     % 点のプロット
%     \fill[purple] (\aOne, \aTwo) circle (1.5pt);
%     \fill[purple] (\aTwo, \aThree) circle (1.5pt);

% \end{tikzpicture}
%  \end{center}
% \end{figure}

\end{document}
