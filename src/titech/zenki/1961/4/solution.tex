\documentclass[../../main.tex]{subfiles}
\usepackage{amsmath}	% required for `\align' (yatex added)
\begin{document}

{\bf [解]}
$A(1,0), B(-1,0), C(t,0)$ ($0 \le t \le 1$) とおくと，与えられた図形は以下のようになる．

\begin{figure}[htb]
\begin{center}
\begin{tikzpicture}[scale=3, >=stealth]
  \draw[->] (-1.3,0) -- (1.5,0) node[right] {$x$};
  \draw[->] (0,-0.3) -- (0,1.5) node[above] {$y$};
  
  \draw[thick] (-0.3,0) circle (0.7);
  \draw[thick] (0.7,0) circle (0.3);
  
  \fill (-0.3,0) circle (1pt);
  \fill (0.7,0) circle (1pt);
  
  % 正確な Q, P の座標定義 (theta = 66度)
  % Q = (-0.3 + 0.7*cos(66), 0.7*sin(66))
  % P = (0.7 + 0.3*cos(66), 0.3*sin(66))
  \coordinate (Q) at ({-0.3 + 0.7*cos(66)}, {0.7*sin(66)});
  \coordinate (P) at ({0.7 + 0.3*cos(66)}, {0.3*sin(66)});

  % PQ の中点 M の座標定義
  \coordinate (M) at ($(P)!0.5!(Q)$);

  % 正しい共通外接線 (P, Q を通る直線)
  % x = -0.5 から x = 1.25 まで描写
  \draw[thick] ($-0.8*(P)+1.8*(Q)$) -- ($1.5*(P)-0.5*(Q)$);

  % 点 P, Q, M のプロットとラベル
  \fill (Q) circle (1pt) node[above left] {$Q$};
  \fill (P) circle (1pt) node[above right] {$P$};
  \fill (M) circle (1pt) node[above right] {$M$};

  % 半径の破線
  \draw[dashed] (-0.3,0) -- (Q);
  \draw[dashed] (0.7,0) -- (P);

  \node[below left] at (-1,0) {$-1$};
  \node[below] at (-0.3,0) {$\frac{-1+t}{2}$};
  \node[below left] at (0,0) {$0$};
  \node[below left] at (0.4,0) {$t$};
  \node[below] at (0.7,0) {$\frac{1+t}{2}$};
  \node[below right] at (1,0) {$1$};
  
  \draw (-0.1,0) arc (0:66:0.2); % 中心-0.3, 半径0.2
  \node at (-0.1,0.2) {$\theta$};
  
  \draw (0.85,0) arc (0:66:0.15); % 中心0.7, 半径0.15
  \node at (0.9,0.1) {$\theta$};
\end{tikzpicture}
\caption{題意の状況}
\end{center}
\end{figure}

題意の2円の方程式は
\begin{align}
   \begin{cases}
    \left(x - \dfrac{1+t}{2}\right)^2 + y^2 = \left(\dfrac{1-t}{2}\right)^2 \\[8pt]
    \left(x - \dfrac{-1+t}{2}\right)^2 + y^2 = \left(\dfrac{1+t}{2}\right)^2
  \end{cases}
  \label{eq:1}
\end{align}
で与えられる．対称性から，$P, Q$ の $y$ 座標が正である時のみを考えよう．
題意より，各円の中心から点$P, Q$に向かう角度は等しいので，これを図のように $\theta\, (0\le\theta\le \pi)$ とおく．
また簡単のため$c = \cos\theta, s = \sin\theta$ とおく．
点$P, Q$における円の接線はそれぞれ
\begin{align}
  \begin{cases}
    c\left(x - \dfrac{1+t}{2}\right) + sy = \dfrac{1-t}{2} \\
    c\left(x - \dfrac{-1+t}{2}\right) + sy = \dfrac{1+t}{2}
  \end{cases}
 \end{align}
で与えられる．これら二つの直線が等しいから定数項を比較して
\begin{align}
   & c \frac{1+t}{2} + \frac{1-t}{2} = c \frac{-1+t}{2} + \frac{1+t}{2} \\
  \therefore 
   & c = t \label{eq:2}
\end{align}
を得る．$0\le\theta\le\pi$より$s\ge 0$だから
\begin{align}
 s = \sqrt{1-t^2}
\end{align}
であって，点$P, Q$の座標は
\begin{align*}
  P\left(\frac{1-t}{2} \cdot t + \frac{1+t}{2}, \frac{1+t}{2}\sqrt{1-t^2}\right) \\
  Q\left(\frac{1+t}{2} \cdot t + \frac{1-t}{2}, \frac{1-t}{2}\sqrt{1-t^2}\right) 
\end{align*}
と書ける．
よって線分 $PQ$ の中点 $M(X,Y)$ は
\begin{align}
  \begin{cases}
    X = \dfrac{1}{2}\left\{\dfrac{t^2+2t-1}{2} + \dfrac{-t^2+2t+1}{2}\right\} = t \\
    Y = \dfrac{1}{2}\left\{\dfrac{1+t}{2} + \dfrac{1-t}{2}\right\}\sqrt{1-t^2} = \dfrac{1}{2}\sqrt{1-t^2} = \dfrac{1}{2}\sqrt{1-X^2}
  \end{cases} 
\end{align}
と書ける．$P, Q$の$y$座標が負の場合は同様に
\begin{align}
  \begin{cases}
    X = t \\
    Y = -\dfrac{1}{2}\sqrt{1-X^2}
  \end{cases} 
\end{align}
だから，\cref{eq:2}より$-1\le t\le 1$であることと合わせて
\begin{align}
 X^2 + 4Y^2 = 1
\end{align}
が求める軌跡である．


{\bf [解説]}

軌跡を求める問題で素直に立式していけば解ける．出てきた答えが正しそうなことは$t$の極限を考えると割と理解しやすい．
まず$t=1$の極限，すなわち右側の円が潰れる時は中点$M$は$(1,0)$であり，逆に$t=-1$の時は$(-1,0)$である．
一方$t=0$の時すなわち二つの円がそれぞれ半径$1/2$の時は$M$は$\left(0,\pm \dfrac{1}{2}\right)$である．
出てきた楕円の方程式はこれらの場合を全て満たしているから，そんなに外してなさそうだということがわかる．

\begin{figure}[htb]
\begin{center}
\begin{tikzpicture}[scale=3, >=stealth]
  \draw[->] (-1.3,0) -- (1.5,0) node[right] {$x$};
  \draw[->] (0,-1.3) -- (0,1.5) node[above] {$y$};
  
  \draw[thick] (-0.3,0) circle (0.7);
  \draw[thick] (0.7,0) circle (0.3);
  
  \fill (-0.3,0) circle (1pt);
  \fill (0.7,0) circle (1pt);
  
  % 正確な Q, P の座標定義 (theta = 66度)
  % Q = (-0.3 + 0.7*cos(66), 0.7*sin(66))
  % P = (0.7 + 0.3*cos(66), 0.3*sin(66))
  \coordinate (Q) at ({-0.3 + 0.7*cos(66)}, {0.7*sin(66)});
  \coordinate (P) at ({0.7 + 0.3*cos(66)}, {0.3*sin(66)});

  % PQ の中点 M の座標定義
  \coordinate (M) at ($(P)!0.5!(Q)$);

  % 正しい共通外接線 (P, Q を通る直線)
  % x = -0.5 から x = 1.25 まで描写
  \draw[thick] ($-0.8*(P)+1.8*(Q)$) -- ($1.5*(P)-0.5*(Q)$);

  % M の軌跡: 楕円 x^2 + 4y^2 = 1 (y >= 0 の領域を描画)
  % x = cos(t), y = 0.5*sin(t) 
  \draw[very thick, red!80!black, domain=0:360, samples=100] 
    plot ({cos(\x)}, {0.5*sin(\x)});
  \node[red!80!black, font=\small, right] at (0.35, -0.52) {$x^2 + 4y^2 = 1$};

  % 点 P, Q, M のプロットとラベル
  \fill (Q) circle (1pt) node[above left] {$Q$};
  \fill (P) circle (1pt) node[above right] {$P$};
  \fill (M) circle (1pt) node[above right] {$M$};

  % 半径の破線
  \draw[dashed] (-0.3,0) -- (Q);
  \draw[dashed] (0.7,0) -- (P);

  \node[below left] at (-1,0) {$-1$};
  \node[below] at (-0.3,0) {$\frac{-1+t}{2}$};
  \node[below left] at (0,0) {$0$};
  \node[below left] at (0.4,0) {$t$};
  \node[below] at (0.7,0) {$\frac{1+t}{2}$};
  \node[below right] at (1,0) {$1$};
  
  \draw (-0.1,0) arc (0:66:0.2); % 中心-0.3, 半径0.2
  \node at (-0.1,0.2) {$\theta$};
  
  \draw (0.85,0) arc (0:66:0.15); % 中心0.7, 半径0.15
  \node at (0.9,0.1) {$\theta$};
\end{tikzpicture}
\caption{$M$の軌跡．}
\end{center}
\end{figure}


\newpage

\end{document}
