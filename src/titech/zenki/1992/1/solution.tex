\documentclass[../../main.tex]{subfiles}
\begin{document}

{\bf [解]}
与えられた関数を
\begin{align}
 f_{k}(x)
  &=\dfrac{x^2-2x+k^2}{x^2+2x+k^2} \\
  &=1-\dfrac{4x}{x^2+2x+k^2} \label{eq:1}
\end{align}
とおく．

$k=0$の時は$f_{0}(x)=1-\dfrac{4}{x+2}$となって$x\to-2+0$の時に$1$以外の整数値をとり不適だから，$k\ne0$である．
この時\cref{eq:1}は
\begin{align}
 f_{k}(x) &=
 \begin{cases}
  1-\dfrac{4}{\left(x+\dfrac{k^2}{x}\right)+2} & \left(x \neq 0\right) \\
  1 & \left(x = 0\right)
 \end{cases} \label{eq:2}
\end{align}
と書ける．表式から$f_{k}(x)$は連続関数であり，かつ$f_{k}(0)=1$だから，$f_{k}(x)$が$1$以外の整数値を取らないための条件は全ての$x$について
\begin{align}
 0 < f_{k}(x) < 2 \label{eq:3}
\end{align}
が成り立つことだから，以下これを満たすように$k$を定める．

$y=x+\dfrac{k^2}{x}$とおくとこのグラフの概形は以下のように成る．

\begin{figure}[htb]
\begin{center}
\begin{tikzpicture}[scale=1, >=stealth]
  % 座標軸
  \draw[->] (-2.5,0) -- (2.5,0) node[right] {$x$};
  \draw[->] (0,-3.5) -- (0,3.5) node[above] {$y$};
  % y=x+k^2/x の2枝 (k=1として図示、最小値2k(x=k)，最大値-2k(x=-k))
  \draw[domain=0.3:2.5, smooth] plot (\x,{\x+1/\x});
  \draw[domain=-2.5:-0.3, smooth] plot (\x,{\x+1/\x});
  \draw[dashed] (0,2) -- (1,2) -- (1,0);
  \draw[dashed] (0,-2) -- (-1,-2) -- (-1,0);
  \node[left] at (0,2) {$2k$};
  \node[right] at (0,-2) {$-2k$};
  \node[below] at (1,0) {$k$};
  \node[below] at (-1,0) {$-k$};
\end{tikzpicture}
\caption{$y=x+\dfrac{k^2}{x}$のグラフの概形}
\end{center}
\end{figure}

% \begin{figure}[htb]
% \begin{center}
% \begin{tikzpicture}[scale=0.75, >=stealth]
%   % 座標軸
%   \draw[->] (-8,0) -- (4,0) node[right] {$y$};
%   \draw[->] (0,-5) -- (0,5) node[above] {$z$};
%   % z=4/(y+2) (漸近線 y=-2); z=1 となるのは y=2, z=-1 となるのは y=-6
%   \draw[domain=0.3:3.5, smooth] plot (\x,{4/(\x+2)}) node[right] {$z=\dfrac{4}{y+2}$};
%   \draw[domain=-7.5:-2.6, smooth] plot (\x,{4/(\x+2)});
%   \draw[dashed] (2,0) -- (2,1) -- (0,1);
%   \draw[dashed] (-6,0) -- (-6,-1) -- (0,-1);
%   \node[below] at (2,0) {$2$};
%   \node[below] at (-6,0) {$-6$};
%   \node[left] at (0,1) {$1$};
%   \node[left] at (0,-1) {$-1$};
% \end{tikzpicture}
% \end{center}
% \caption{$z=\dfrac{4}{y+2}$のグラフ}
% \end{figure}

\cref{eq:3}の条件に\cref{eq:2}を代入して
\begin{align}
 \begin{split}
 & 0 < 1-\dfrac{4}{y+2} < 2 \\
 & -1 < \dfrac{4}{y+2} < 1 \\
 & -(y+2)^2 < 4(y+2) < (y+2)^2 \\ 
 & (y+2)(-4-y-2)<0, \quad (y+2)(y+2-4) > 0 \\
 & (y+2)(-y-6)<0, \quad (y+2)(y-2)>0 \\  
 & 2 < y \quad \text{or} \quad y < -6
 \end{split}
\end{align}
である．これが全ての$x$で成立すれば良い．
$y$のグラフから$y>0$での$y$の最小値は$2k$, $y<0$での$y$の最大値は$-2k$だからこの条件は
\begin{align}
\begin{split}
&2<2k \ \text{and}\ -2k<-6 \\
&\iff 3<k
\end{split}
\end{align}
である．


\newpage

\end{document}
