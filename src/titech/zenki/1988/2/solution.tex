\documentclass[../../main.tex]{subfiles}
\begin{document}

{\bf [解1]}
\paragraph{(1)}
$f'(x)=2ax+b$である．題意より$x=0,1,-1$で$|f(x)|\le 1$だから
\begin{align}
 \begin{cases}
 |f(0)| = |c| \le 1 \\
 |f(1)| = |a+b+c| \le 1 \\
 |f(-1)| = |a-b+c| \le 1 
 \end{cases}
\end{align}
となる．絶対値を外して
\begin{align}
  -1 \le c \le 1 \label{eq:1}\\
  -1 \le a+b+c \le 1 \label{eq:2}\\
  -1 \le a-b+c \le 1 \label{eq:3}
\end{align}
である．ここで
\begin{align}
 \dfrac{3}{2}\text{\cref{eq:2}} + \dfrac{1}{2}\text{\cref{eq:3}} - 2\text{\cref{eq:1}} \label{eq:4}
\end{align}
を計算すると
\begin{align}
 -4 \le 2a + b \le 4
\end{align}
である．$f'(1)=2a+b$だから
\begin{align}
 |f'(1)| \le 4
\end{align}
であり，よって題意は示された．

% 元の解
% $g(x)=ax^2+bx$とおくと，$f'(x)=g'(x)=2ax+b$である．又，題意から
% \begin{align}
% -2\le g(1)-g(0)\le2, \qquad -2\le g(-1)-g(0)\le2 \iff -2\le\pm a+b\le2 \quad\cdots\text{①}
% \end{align}
% が成立する．さらに，$g'(1)=2a+b$である．ここで，題意が成立しないと仮定すると，
% \begin{align}
% |2a+b|>4 \quad\cdots\text{②}
% \end{align}
% ところが，①$\wedge$②をみたす$(a,b)$は存在せず，不適．よって背理法から題意は成立．

\paragraph{(2)}

$|f'(1)|=4$が成り立つということは(1)より\cref{eq:4}で等号が成立するので\cref{eq:1,eq:2,eq:3}より
\begin{align}
  c = \mp 1 \label{eq:5}\\
  a+b+c = \pm 1 \label{eq:6}\\
  a-b+c = \pm 1 \label{eq:7}
\end{align}
である．ただし複合同順である．\cref{eq:6,eq:7}を辺々足し引きして
\begin{align}
 \begin{cases}
   2a+2c = \pm 2 \quad \therefore a+c = \pm 1 \\
   2b = 0 \quad \therefore b = 0 
 \end{cases}\label{eq:8}
\end{align}
である．さらに\cref{eq:5}を\cref{eq:8}の第一式に代入して
\begin{align}
 a = \pm 2
\end{align}
である．以上より
\begin{align}
 (a,b,c) = (\pm 2,0,\mp 1)
\end{align}
である．ただし複合同順である．この時関数$f(x)$は
\begin{align}
 f(x) = \pm \left(2x^2-1\right)
\end{align}
であるが，これは題意の条件$|f(x)|\le 1\, (|x|\le 1)$を満たすから十分である．
以上より求める関数は
\begin{align}
 f(x) \pm \left(2x^2-1\right)
\end{align}
である．

{\bf [解2]}

[解1]では不等式の性質を使って$x=0,\pm 1$の値のみを使って解いたが，もっとゴリゴリに二次方程式の性質を使って解くこともできるのでその例も閉めそう．受験だとこういう解答の方が途中で事故りにくいので安全かもしれない．

\paragraph{(1)}
$a=0$での成立は明らかだから以下対称性から$a>0$とする．$f(x)$は二次方程式だから平方完成すると
\begin{align}
 f(x)=a\left(x+\dfrac{b}{2a}\right)^2-\dfrac{b^2}{4a}+c
\end{align}
であり，区間内での最大最小値は軸の位置$x=-\dfrac{b}{2a}$の値によって以下のようになる．
\begin{table}[htb]
  \centering
\begin{tabular}{c|c|c|c}
 & 1. $-1\le-\dfrac{b}{2a}\le0$ & 2. $0\le-\dfrac{b}{2a}\le1$ & 3. otherwise \\
\hline
$\max f$ & $f(1)=a+b+c$ & $f(-1)=a-b+c$ & $f(\pm 1)$のうち小さくない方 \\
\hline
$\min f$ & $f\left(-\dfrac{b}{2a}\right)=-\dfrac{b^2}{4a}+c$ & $f\left(-\dfrac{b}{2a}\right)=-\dfrac{b^2}{4a}+c$ & $f(\pm 1)$のうち大きくない方
\end{tabular}
\end{table}

1と2の対称性から1と3だけ考えれば良い．

\subparagraph{1. の時（$-1\le-\dfrac{b}{2a}\le0$）}

$a>0$から$0\le b\le 2a$である．$|f(x)| \le 1$より$-1 \le \min f, \max f \le 1$であるから
\begin{align}
  & -1 \le -\dfrac{b^2}{4a}+c \le 1 \\
  & -1 \le a+b+c \le 1 
\end{align}
である．辺々引き算して
\begin{align}
 & -2 \le a+b+\dfrac{b^2}{4a} \le 2 \\
 & -8a \le 4a^2+4ab+b^2 \le 8a \\
 & -8a \le (2a+b)^2 \le 8a \\ 
\therefore
 &\left|2a+b\right| \le 2\sqrt{2a} \\
\therefore
 -2a - 2\sqrt{2a} \le b \le 2a + 2\sqrt{2a}
\end{align}
である．これと$0\le b\le 2a$を合わせて$(a,b)$平面に図示すると以下の斜線部である．

\begin{figure}[htb]
 \begin{center}
  \begin{tikzpicture}[scale=1.2, >=stealth]

  % --- 1. 座標軸 ---
  \draw[->, thick] (-0.5,0) -- (3.2,0) node[right] {$a$};
  \draw[->, thick] (0,-0.5) -- (0,3.2) node[above] {$b$};
  \node[below left] at (0,0) {$O$};

  % --- 2. 領域の描画 (ハッチング) ---
  % 境界1: b = 0
  % 境界2: b = 2a (直線)
  % 境界3: 2a + b = 2*sqrt(2a)  <=>  b = 2*sqrt(2a) - 2a (放物線様曲線)
  
  % 領域の塗りつぶし
  \fill[pattern=north east lines, pattern color=blue!50]
    (0,0) -- plot[domain=0:2, samples=50] (\x, {2*sqrt(2*\x) - 2*\x}) -- (0,0);

  % --- 3. 境界線の描画 ---
  % b = 2a の直線 (上側の境界)
  \draw[thick, blue!80!black, domain=0:1.0] plot (\x, {2*\x}) 
    node[above] {$b = 2a$};

  % 2a + b = 2*sqrt(2a) の曲線 (右側の境界)
  % a=2 で b=0 となり a 軸に接する
  \draw[ultra thick, blue!80!black, domain=0:2.2, samples=100] 
    plot (\x, {2*sqrt(2*\x) - 2*\x}) node[right, font=\small] {$2a+b = 2\sqrt{2a}$};

  % --- 4. 直線 2a + b = k の描画 ---
  % (i) 一般の k (k = 2)
  \draw[dashed, red!60, thick, domain=-0.2:1.3] plot (\x, {2 - 2*\x}) 
    node[below right, font=\small] {$2a+b=k$};

  % (ii) 最大値を与える k = 4 (点 (2,0) で領域に接する)
  %\draw[ultra thick, red!80!black, domain=0.8:2.3] plot (\x, {4 - 2*\x}) 
  %  node[above right, red!80!black] {$2a+b=k$};

  % --- 5. 接点 (2,0) のプロットとラベル ---
  \fill[red!80!black] (2,0) circle (2pt) node[below, font=\small] {$2$};

\end{tikzpicture}
\caption{$(a,b)$のとりうる領域}
 \end{center}
\end{figure}


$f'(1)=2a+b$のとりうる範囲は直線$2a+b=k$と斜線部の領域の交点だから
\begin{align}
  0\le2a+b\le4
\end{align}
である．右の等号成立は$(a,b)=(2,0)$の時である．

\subparagraph{3の時}

この時は軸の条件から，
\begin{align}
-\dfrac{b}{2a}\le-1 \ \text{or}\ 1\le-\dfrac{b}{2a}
\end{align}
である．$a>0$だから
\begin{align}
2a\le b \ \text{or}\ b\le-2a \label{eq:9}
\end{align}
である．又$|f(\pm)| \le 1$より
\begin{align}
 & -1 \le a+ b + c \le 1 \\
 & -1 \le a- b + c \le 1
\end{align}
である．辺々足し引きして，
\begin{align}
 -1 \le b \le 1 \label{eq:10}
\end{align}
である．\cref{eq:9,eq:10}を図示すると下図斜線部である（境界は$a=0$のみ含まず）．

\begin{figure}[htb]
\begin{center}
\begin{tikzpicture}[scale=2, >=stealth]

  % --- 1. 座標軸 ---
  \draw[->, thick] (-0.5,0) -- (2.2,0) node[right] {$a$};
  \draw[->, thick] (0,-1.8) -- (0,1.8) node[above] {$b$};
  \node[below left] at (0,0) {$O$};

  % --- 2. 帯状領域 -1 <= b <= 1 の平行線 ---
  \draw[dashed, gray] (0, 1.0) node[left, black, font=\small] {$1$} -- (2.0, 1.0) 
    node[right, black, font=\small] {$b=1$};
  \draw[dashed, gray] (0, -1.0) node[left, black, font=\small] {$-1$} -- (2.0, -1.0) 
    node[right, black, font=\small] {$b=-1$};
  \draw[dashed, gray] (0.5, 1.0)  -- (0.5, -1.0);


  % --- 3. 領域の描画 (ハッチング) ---
  % (i) b >= 2a かつ b <= 1 (左上の小さな三角形)
  \fill[pattern=north east lines, pattern color=blue!50]
    (0,0) -- (0.5,1.0) -- (0,1.0) -- cycle;

  % (ii) b <= -2a かつ b >= -1 (右下の三角形)
  \fill[pattern=north east lines, pattern color=blue!50]
    (0,0) -- (0.5,-1.0) -- (0.0,-1.0) -- cycle;

  % --- 4. 境界線 b = ±2a の描画 ---
  \draw[thick, blue!80!black, domain=0:0.8] plot (\x, {2*\x}) 
    node[above left] {$b = 2a$};
  \draw[thick, blue!80!black, domain=0:0.8] plot (\x, {-2*\x}) 
    node[below left] {$b = -2a$};

  % a=0 (y軸) は除く白抜き丸
  \draw[fill=white, thick] (0,0) circle (1.5pt);

  % --- 5. 直線 2a + b = k の描画 ---
  % (i) 端点 (0.5, 1) を通るとき: 2(0.5)+1 = 2 => k = 2
  \draw[ultra thick, red!80!black, domain=-0.2:1.2] plot (\x, {2 - 2*\x}) 
    node[below right, red!80!black, font=\small] {$2a+b=k$};
  
  \fill[red!80!black] (0.5,1.0) circle (1.8pt) node[right, font=\small] {$(0.5, 1)$};

\end{tikzpicture}
\caption{$(a,b)$のとりうる領域}
\end{center} 
\end{figure}


従って$f'(1)=2a+b$のとりうる範囲は，直線$2a+b=k$と斜線部の領域の交点を考えて
\begin{align}
-1<2a+b\le2
\end{align}
だから$|2a+b|<4$である．
\casesend

以上から，$|2a+b|\le4$は成立する．

\paragraph{(2)}
(1)で等号が成立する場合を考えればよく，1と2の場合分けの対称性も加味して$(a,b)=(\pm2,0)$の時である．（以下略）


\newpage

\end{document}
