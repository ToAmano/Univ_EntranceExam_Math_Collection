\documentclass[../../../../main.tex]{subfiles}
% \documentclass[a4paper,10pt]{ltjsarticle}
\begin{document}
\rhead{東京大学後期$1996$年$2$番}
\setcounter{equation}{0}
\begin{oframed}
  \input{../../../problems/1996/2/problem.tex}
\end{oframed}
\setlength{\columnseprule}{0.4pt}
\begin{multicols}{2}
  {\bf[解]}

  まず四面体を$xyz$座標空間に配置することを考える．
  中点連結定理から，
  \begin{align*}
    \begin{dcases}
      MN = a \\
      ML = c \\
      NL = b
    \end{dcases}
  \end{align*}
  であり，3辺の長さが等しいことから
  \begin{align*}
    \triangle AMN \equiv \triangle MCL \equiv \triangle NLB \equiv \triangle LNM
  \end{align*}
  である．

  また，三角形ABCが鋭角三角形であることから
  \begin{align}
    \begin{dcases}
      a^2 + b^2 > c^2 \\
      a^2 + c^2 > b^2 \\
      b^2 + c^2 > a^2
    \end{dcases} \label{1996-2:eq:1}
  \end{align}
  が成り立つ．

  \begin{figure}[H]
    \centering
    \begin{tikzpicture}[scale=1.2]
      \begin{axis}[
          axis lines=center, % 軸を中央で交差させる
          ticks=none,        % 目盛りを非表示
          xlabel=$x$,
          ylabel=$y$,
          zlabel=$z$,
          xmin=0,xmax=5,
          ymin=0,ymax=5,
          zmin=0,zmax=5,
          view={110}{25},    % 図の視点を設定 (方位角, 仰角)
          enlargelimits=0.1, % 描画範囲に余白を追加
          axis equal,        % 全ての軸でスケールを等しくする
          z buffer=sort,     % 描画要素の前後関係を自動でソート
          clip=false,
        ]

        %--- 直方体の寸法を定義 ---%
        \def\X{4.5}
        \def\Y{4.5}
        \def\Z{5}

        %--- 頂点の座標を定義 ---%
        \coordinate (O) at (0,0,0);
        \coordinate (A) at (\X,0,0);
        \coordinate (Bprime) at (0,\Y,0);
        \coordinate (Cprime) at (\X,\Y,0);
        \coordinate (Dprime) at (0,0,\Z);
        \coordinate (Eprime) at (\X,0,\Z);
        \coordinate (Fprime) at (\X,\Y,\Z);
        \coordinate (Gprime) at (0,\Y,\Z);

        %--- 直方体の辺を描画 (破線) ---%
        \draw[dashed, gray]
        (O) -- (A) -- (Cprime) -- (Bprime) -- cycle
        (Dprime) -- (Eprime) -- (Fprime) -- (Gprime) -- cycle
        (O) -- (Dprime)
        (A) -- (Eprime)
        (Bprime) -- (Gprime)
        (Cprime) -- (Fprime);

        %--- 四面体 (または強調された線) を描画 (実線) ---%
        % 図に合わせて頂点A, B', D', F'を結ぶ線を実線で描画
        \draw[solid, thick]
        (A) -- (Fprime)
        (Bprime) -- (Fprime)
        (Dprime) -- (Fprime);
        \draw[dashed, thick] (A) -- (Bprime) -- (Dprime) --cycle;

        %--- 頂点ラベルを配置 ---%
        \node at (O) [below left] {$O$};
        \node at (A) [below] {$A'$};
        \node at (Bprime) [right] {$B'$};
        \node at (Dprime) [above left] {$D'$};
        \node at (Fprime) [above right] {$F'$};

        %--- 薄い補助ラベルを配置 ---%
        \node[text=gray] at (Cprime) [below right=2pt] {$C'$};
        \node[text=gray] at (Eprime) [above] {$E'$};
        \node[text=gray] at (Gprime) [above] {$G'$};

        %--- X, Y, Z ラベルを配置 ---%
        % 手書きの位置に合わせて配置
        \node[left] at ($(Bprime)!0.5!(Cprime)$) {$X$};
        \node at ($(A)!0.5!(Cprime)$) {$Y$};
        \node at (0, \Y*0.3, \Z*0.5) {$Z$};

      \end{axis}
    \end{tikzpicture}
    \caption{空間座標に四面体を配置した様子}
    \label{1996-2:fig:1}
  \end{figure}

  ここで，座標空間において，
  8点 $O(0,0,0)$，$A'(X,0,0)$， $B'(0,Y,0)$， $C'(X,Y,0)$， $D'(0,0,Z)$，
  $E'(X,0,Z)$， $F'(X,Y,Z)$， $G'(0,Y,Z)$ を考える．
  ただし$X,Y,Z>0$とする．これら8点は辺の長さが$X，Y，Z$の直方体を形成する．
  面の対角線の長さについて
  \begin{align*}
    \begin{dcases}
      AF' = B'D' = \sqrt{Y^2+Z^2} \\
      BF' = A'D' = \sqrt{X^2+Z^2} \\
      A'B' = D'F' = \sqrt{X^2+Y^2}
    \end{dcases}
  \end{align*}
  だから，四面体 $AF'DB'$ の4面は全て合同な三角形である．

  従って
  \begin{align}
     & \begin{dcases}
         a = \sqrt{Y^2+Z^2} \\
         b = \sqrt{X^2+Z^2} \\
         c = \sqrt{X^2+Y^2}
       \end{dcases}                 \\
    \therefore
     & \begin{dcases}
         x = \sqrt{\frac{-a^2+b^2+c^2}{2}} \\
         y = \sqrt{\frac{a^2-b^2+c^2}{2}}  \\
         z = \sqrt{\frac{a^2+b^2-c^2}{2}}
       \end{dcases} \label{1996-2:eq:2}
  \end{align}
  となるよう$X,Y,Z$ を設定し，
  $F'$を$A(=B=C)$，
  $D'$を$N$，
  $A'$を$L$，
  $B'$を$M$
  でおきかえて考えれば良い．
  \cref{1996-2:eq:1}から必ずこのような$x,y,z$が存在する．

  この時，$Q (X, \frac{Y}{2}, \frac{Z}{2})$
  $P (0, \frac{Y}{2}, \frac{Z}{2}) \cdots ②$

  (1)
  この座標系で考えると，$M(0,Y,0)$，$N(0,0,Z)$の中点Pは
  \begin{align*}
    P\left(0,\frac{Y}{2},\frac{Z}{2}\right)
  \end{align*}
  であり，
  $B(X,Y,Z)$，$L(X,0,0)$の中点Qは
  \begin{align*}
    Q\left(X,\frac{Y}{2},\frac{Z}{2}\right)
  \end{align*}
  だから，線分PQの長さは\cref{1996-2:eq:2}から
  \begin{align*}
    \overline{PQ} = X = \sqrt{\frac{-a^2+b^2+c^2}{2}}
  \end{align*}
  である．  $\cdots$(答)
  \vspace{10pt}

  (2)
  四面体の体積Vは 立方体O'A'C'B'D'E'F'G'の体積からまわりの4つの三角錐の体積を引いたものである．
  よって
  \begin{align*}
    V
     & = XYZ - 4 \cdot \frac{1}{6}XYZ \\
     & = \frac{1}{3}XYZ
  \end{align*}
  である．\cref{1996-2:eq:2}を代入して
  \begin{align*}
    V
     & = \frac{\sqrt{2}}{12} \sqrt{(-a^2+b^2+c^2)(a^2-b^2+c^2)(a^2+b^2-c^2)}
  \end{align*}
  である．$\cdots$(答)
  \vspace{10pt}
  {\bf[解説]}
  % TODO :: 中点連結定理

  \newpage
\end{multicols}
\end{document}