\documentclass[../../../../main.tex]{subfiles}
\begin{document}

$xy$ 平面上の $y \geqq x^2$ で表される領域を $D$ とする．
$D$ に含まれる1辺の長さ $t$ の正方形で，各辺が座標軸と平行または $45^\circ$ の角をなすものをすべて考える．

このとき，これらの正方形の中心の $y$ 座標の最小値を $t$ の関数として表し，そのグラフをかけ．

\begin{figure}[htbp]
  \centering
  \begin{tikzpicture}[scale=1.5, >=stealth]
    \draw[->] (-2.2,0) -- (2.2,0) node[right] {$x$};
    \draw[->] (0,-0.5) -- (0,3.2) node[above] {$y$};
    \node[below left] at (0,0) {$O$};

    \draw[thick, domain=-1.7:1.7, samples=100] plot (\x, {\x*\x});

    \draw[thick] (-1.1, 0.7) rectangle (0.3, 2.1);
    \fill (-0.4, 1.4) circle (1.5pt);

    \begin{scope}[shift={(0.2, 2.1)}, rotate=45]
      \draw[thick] (-0.6, -0.6) rectangle (0.6, 0.6);
      \fill (0,0) circle (1.5pt);
    \end{scope}
  \end{tikzpicture}
\end{figure}

\end{document}
