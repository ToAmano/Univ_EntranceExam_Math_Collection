\documentclass[../../../../main.tex]{subfiles}

\begin{document}

\section*{1988年 東京大学 理科}

\subsection*{第1問}

(解答なし)

\subsection*{第2問}

[解] 空間座標を設定し, $T$ の4頂点を
\[
A(0,0,0), \quad B\left(0, \frac{1}{\sqrt{2}}, \frac{1}{\sqrt{2}}\right), \quad C\left(\frac{1}{\sqrt{2}}, 0, \frac{1}{\sqrt{2}}\right), \quad D\left(\frac{1}{\sqrt{2}}, \frac{1}{\sqrt{2}}, 0\right)
\]
とする. 平面 $\alpha$ の法線ベクトル $\vec{n} = \begin{pmatrix} a_1 \\ a_2 \\ a_3 \end{pmatrix}$ とする. ただし
\[
a_1^2 + a_2^2 + a_3^2 = 1, \quad 0 \le a_1, a_2, a_3 \quad \dots \text{①}
\]
として考える. 面 $ABC, ACD, ABD, BCD$ を $S_1, S_2, S_3, S_4$ とおくと, $S_1 \sim S_4$ の法線ベクトルは, 順に
\[
\vec{n}_1 = \begin{pmatrix} 1 \\ -1 \\ -1 \end{pmatrix}, \quad \vec{n}_2 = \begin{pmatrix} -1 \\ 1 \\ -1 \end{pmatrix}, \quad \vec{n}_3 = \begin{pmatrix} -1 \\ -1 \\ 1 \end{pmatrix}, \quad \vec{n}_4 = \begin{pmatrix} 1 \\ 1 \\ 1 \end{pmatrix}
\]
だから, 面 $S_k$ と $\alpha$ のなす角 $\theta_k$ として,
\[
\cos\theta_1 = \frac{a_1-a_2-a_3}{\sqrt{3}}, \quad \cos\theta_2 = \frac{-a_1+a_2-a_3}{\sqrt{3}}, \quad \cos\theta_3 = \frac{-a_1-a_2+a_3}{\sqrt{3}}, \quad \cos\theta_4 = \frac{a_1+a_2+a_3}{\sqrt{3}}
\]
だから, 求める面積 $S$ として, $T$ の一面の面積 $S_0$ から,
\begin{align*}
2S &= \frac{\sqrt{3}}{4} \sum_{k=1}^4 |\cos\theta_k| \\
&= \frac{1}{4} \left[ |a_1+a_2-a_3| + |a_1-a_2+a_3| + |-a_1+a_2+a_3| + |a_1+a_2+a_3| \right] \quad \dots \text{②}
\end{align*}
対称性から, $a_1 \le a_2 \le a_3 \quad \dots \text{③}$ とすると, ①とあわせて,
\[
8S = |a_1+a_2-a_3| + a_1+a_2+3a_3 \quad \dots \text{③}'
\]
である. $a_1+a_2-a_3$ の正負で場合分けする.

\paragraph{$1^\circ \ a_1+a_2 \ge a_3$ の時}
③'から, $8S = 2(a_1+a_2+a_3) \quad \therefore S = \frac{1}{4}(a_1+a_2+a_3)$ となる. ① $\land$ ③ $\land a_1+a_2 \ge a_3$ を図示して, 右図斜線部(境界含む)だから.

平面 $k = a_1+a_2+a_3$ との共有点条件より,

\begin{center}
\begin{tikzpicture}[scale=2, >=stealth]
  \draw[->] (0,0,0) -- (1.3,0,0) node[right] {$a_1$};
  \draw[->] (0,0,0) -- (0,1.3,0) node[above] {$a_3$};
  \draw[->] (0,0,0) -- (0,0,1.3) node[below left] {$a_2$};
  
  \coordinate (P) at (0,1,0);
  \coordinate (Q) at (0,0.707,0.707);
  \coordinate (R) at (0.577,0.577,0.577);
  \coordinate (S) at (0.5,0.707,0.5);

  \draw (0,0,0) node[below left] {$O$};
  
  \draw[dashed] (1,0,0) arc (0:90:1);
  \draw (0,1,0) arc (90:180:1);
  
  \draw[thick, fill=gray!30] (Q) to[out=-20,in=200] (R) to[out=40,in=-40] (S) to[out=140,in=-10] cycle;
  
  \node[left] at (P) {$a_3$};
  \node[above right] at (R) {$R$};
  \node[left] at (Q) {$Q$};
  \node[right] at (S) {$S$};
  
  \node[below] at (0.4,0,0.4) {$a_1+a_2=a_3$};
  \node[above right] at (0.3,0.8,0.3) {$a_1=a_2$};
  \node[right] at (0,0.5,0.5) {$a_2=a_3$};
\end{tikzpicture}
\end{center}

\[
\begin{cases}
Q \text{で} \min k = \sqrt{2} \\
S \text{で} \max k = \sqrt{3}
\end{cases}
\]
をとり, その間もれ無くとるから,
\[
\frac{\sqrt{2}}{4} \le S \le \frac{\sqrt{3}}{4} \quad \dots \text{④}
\]

\paragraph{$2^\circ \ a_1+a_2 \le a_3$ の時}
③'から, $8S = 4a_3 \quad \therefore S = \frac{1}{2}a_3$ となる. $1^\circ$ と同様に, $(a_1,a_2,a_3)$ の存在範囲は右図斜線部だから (境界含む)
\[
\frac{1}{\sqrt{6}} \le a_3 \le 1
\]
となり,
\[
\frac{1}{2\sqrt{6}} \le S \le \frac{1}{2} \quad \dots \text{⑤}
\]

\begin{center}
\begin{tikzpicture}[scale=2, >=stealth]
  \draw[->] (0,0,0) -- (1.3,0,0) node[right] {$a_1$};
  \draw[->] (0,0,0) -- (0,1.3,0) node[above] {$a_3$};
  \draw[->] (0,0,0) -- (0,0,1.3) node[below left] {$a_2$};
  
  \coordinate (P) at (0,1,0);
  \coordinate (Q) at (0,0.707,0.707);
  \coordinate (R) at (0.408,0.816,0.408);
  
  \draw[thick, fill=gray!30] (P) to[out=-60,in=90] (Q) to[out=30,in=-120] (R) to[out=110,in=-30] cycle;
  
  \node[above] at (P) {$P$};
  \node[left] at (Q) {$Q$};
  \node[right] at (R) {$R$};
\end{tikzpicture}
\end{center}

以上④, ⑤から,
\[
\frac{1}{2\sqrt{6}} \le S \le \frac{1}{2}
\]

\subsection*{第3問}

[解] $C$ を $\begin{pmatrix} a \\ b \end{pmatrix}$ だけ平行移動した図形は,
\[
y-b = (x-a)^3 - (x-a) \quad (-1+a \le x \le 1+a) \quad \dots \text{①}
\]
である. これと $y=x^3-x$ との共有点がただ1つある時, まず $y$ を消去して,
\begin{align*}
x^3 - x - b &= (x-a)^3 - (x-a) \\
-x - b &= -3a x^2 + (3a^2-1) x - a^3 + a \\
b &= 3a x^2 - 3a^2 x + a^3 - a \equiv f(x)
\end{align*}
これが, $-1 \le x \le 1$ かつ $-1+a \le x \le 1+a$ にただ1つ解を持つ条件を調べる.
右図及び, $f(x)$ の軸が $x = \frac{a}{2}$ であることから, 条件は
\[
f\left(\frac{a}{2}\right) = b \iff b = \frac{1}{4}a^3 - a \quad \dots \text{②}
\]
この時 $(a,b) \ne (0,0)$ だから $a \ne 0$ で, ①に代入して,
\[
y = (x-a)^3 - (x-a) + \frac{1}{4}a^3 - a \quad (-1+a \le x \le 1+a) \quad \dots \text{③}
\]
で, $P(X, Y)$ とおくと ③が $P$ を通ることから,
\[
Y = (X-a)^3 - (X-a) + \frac{1}{4}a^3 - a \quad (-1+a \le X \le a+1) \quad \dots \text{④}
\]
これをみたす $a(\ne 0)$ がただ1つ存在する条件を考える. ④の右辺を $g(a)$ とおく.
\begin{align*}
g(a) &= -\frac{3}{4}a^3 + 3X a^2 - 3X^2 a + X^3 - X \\
g'(a) &= -\frac{9}{4}a^2 + 6X a - 3X^2 \\
&= -\frac{3}{4}(3a-2X)(a-2X)
\end{align*}
したがって, 右表とあわせて, ④が $a$ について3実解を持つには, $-1 < X < 1 \ (X \ne 0)$ が必要で, $X$ の正負で場合分けする.

\paragraph{$1^\circ \ -1 < X < 0$}

\begin{center}
\begin{tabular}{c|c|c|c|c|c|c|c}
$a$ & $X-1$ & $\dots$ & $2X$ & $\dots$ & $\frac{2}{3}X$ & $\dots$ & $X+1$ \\ \hline
$g'$ & & $-$ & $0$ & $+$ & $0$ & $-$ & \\ \hline
$g$ & & $\searrow$ & 極小 & $\nearrow$ & 極大 & $\searrow$ &
\end{tabular}
\end{center}

したがって, ④が $a \ne 0$ に3実解を持つ条件は, $\alpha, \beta$ のうち, 小さくない方 $\max(\alpha,\beta)$, 大きくない方 $\min(\alpha,\beta)$ として
\[
\min\left(g(2X), g(X+1)\right) \le Y \le \max\left(g(X-1), g\left(\frac{2}{3}X\right)\right) \land g(0) \ne Y \quad \dots \text{⑤}
\]
ただし, $Y \ne g(2X), Y \ne g(X+1)$ である.

\paragraph{$2^\circ \ 0 < X < 1$}

\begin{center}
\begin{tabular}{c|c|c|c|c|c|c|c}
$a$ & $X-1$ & $\dots$ & $\frac{2}{3}X$ & $\dots$ & $2X$ & $\dots$ & $X+1$ \\ \hline
$g'$ & & $-$ & $0$ & $+$ & $0$ & $-$ & \\ \hline
$g$ & & $\searrow$ & 極小 & $\nearrow$ & 極大 & $\searrow$ &
\end{tabular}
\end{center}

したがって, ④が $a \ne 0$ に3実解を持つ条件は
\[
\min\left(g\left(\frac{2}{3}X\right), g(X+1)\right) \le Y \le \max\left(g(2X), g(X-1)\right) \land g(0) \ne Y \quad \dots \text{⑥}
\]
ただし, $Y \ne g(2X), Y \ne g\left(\frac{2}{3}X\right)$ である.

$1^\circ$ および $2^\circ$:
\begin{align*}
g(X+1) &= \frac{1}{4}(X+1)^3 - (X+1) + (-1)^3 - (-1) \\
&= \frac{1}{4}(X+1)(X+3)(X-1) \quad \dots \text{⑦} \\
g(X-1) &= \frac{1}{4}(X-1)^3 - (X-1) \\
&= \frac{1}{4}(X-1)(X+1)(X-3) \quad \dots \text{⑧} \\
g(2X) &= \frac{1}{4} \cdot 8 X^3 - 2X + (-X)^3 - (-X) \\
&= X^3 - X \quad \dots \text{⑨} \\
g\left(\frac{2}{3}X\right) &= \frac{1}{4}\left(\frac{2}{3}X\right)^3 - \left(\frac{2}{3}X\right) + \left(\frac{1}{3}X\right)^3 - \left(\frac{1}{3}X\right) \\
&= \frac{1}{9}X^3 - X \quad \dots \text{⑩}
\end{align*}
又, $g(0) = X^3 - X$ から, もとめるのは下図斜線部 (境界は実線のみ含む).

\begin{center}
\begin{tikzpicture}[scale=1.5, >=stealth]
  \draw[->] (-1.5,0) -- (1.5,0) node[right] {$x$};
  \draw[->] (0,-1.5) -- (0,1.5) node[above] {$y$};
  \node[below left] at (0,0) {$O$};

  \draw[domain=-1.2:1.2, samples=100, thick] plot (\x, {\x*\x*\x - \x}) node[right] {①};
  \draw[domain=-1.2:1.2, samples=100, dashed] plot (\x, {(1/9)*\x*\x*\x - \x}) node[right] {④};
  \draw[domain=-1.2:1.1, samples=100, dotted] plot (\x, {0.25*(\x+1)*(\x+3)*(\x-1)}) node[above] {③};
  \draw[domain=-1.1:1.2, samples=100, dashdot pattern=on 3pt off 2pt] plot (\x, {0.25*(\x-1)*(\x+1)*(\x-3)}) node[below] {②};
  
  \node at (-1,0) [below left] {$-1$};
  \node at (1,0) [below right] {$1$};
\end{tikzpicture}
\end{center}

\paragraph{[解2] (前半部, ②をみちびく)}
$\vec{A} = \begin{pmatrix} a \\ b \end{pmatrix}$ だけ平行移動とする. $OA$ の中点 $M\left(\frac{1}{2}a, \frac{1}{2}b\right)$ とする. $C$ の任意の点 $(x,y)$ の, $M$ に関する対称点を $(x',y')$ とすると,
\[
x = a - x', \quad y = b - y'
\]
だから, $y = x^3 - x$ に代入して
\[
y - b = (x'-a)^3 - (x'-a)
\]
つまり, $(x',y')$ は $C'$ 上の点である. したがって, $C, C'$ は $M$ を中心として点対称である.
よって, $C, C'$ の共有点も $M$ を中心として点対称だから,
\[
\text{「共有点がただ1つ} \iff \text{共有点が} M\text{」}
\]
となり,
\[
\frac{b}{2} = f\left(\frac{a}{2}\right) \quad \therefore b = \frac{1}{4}a^3 - a
\]
を得る.

\subsection*{第4問}

[解] $n$ 回目に到達した格子点 ($(0,0)$をのぞく) を $P_n$ とする ($n \in \mathbb{N}$). 原点を $P_0$ として, 題意より
\[
\vec{P_{n-1} P_n} = \begin{pmatrix} S^{n-1} a \\ 1 \end{pmatrix}
\]
だから,
\begin{align*}
\vec{OP_n} &= \sum_{k=1}^n \vec{P_{k-1} P_k} = \sum_{k=1}^n \begin{pmatrix} S^{k-1} a \\ 1 \end{pmatrix} \\
&= \begin{cases} \begin{pmatrix} \frac{1}{a} \frac{1-(1/S)^n}{1-1/S} \\ n \end{pmatrix} & (S \ne 1) \\[10pt] \begin{pmatrix} \frac{n}{a} \\ n \end{pmatrix} & (S=1) \end{cases}
\end{align*}
よって, $P_n$ の $x$ 座標は, 単調増加で, $n \to \infty$ の時
\[
\begin{cases}
0 < S < 1 \text{ なら } +\infty \\
S = 1 \text{ なら } +\infty \\
1 < S \text{ なら } \frac{1}{a} \frac{1}{1-1/S}
\end{cases}
\]
だから, $P$ のキセキの連続性から,
\[
0 < S \le 1 \quad \text{or} \quad \frac{1}{a} \frac{S}{S-1} > b \quad (S > 1)
\]

\subsection*{第5問}

[解] 対称性から $P(1, 0, 1)$ としても良い. この時, $P$ での $V$ の接平面は, $(2-x^2)' = -2x$ から
\[
z = -2(x-1) + 1
\]
だから, $xz$ 平面では $z = \frac{3}{2}$ となり, 右下図のようになる.
右下図のように $\theta$ をおくと, $C$ の半径 $r$ として, 題意から
\[
2\pi r = 2\pi \quad \dots \text{①}
\]
又
\[
r \cdot \theta = \frac{3}{2} \quad \dots \text{②}
\]
①, ②から,
\[
\frac{3}{2}\pi = \frac{\theta}{\cos\theta} \quad \dots \text{③}
\]
この右辺は $0 < \theta < \pi/2$ で単調増加だから, $\theta = \pi/3$ が③をみたし, 唯一の解である. したがって, この時 $r = 3$ となり, もとめる長さは
\[
(2\pi - 2\theta) \cdot r = 4\pi
\]

\begin{center}
\begin{tikzpicture}[scale=1.2, >=stealth]
  \draw[->] (-0.5,0) -- (2.2,0) node[right] {$x$};
  \draw[->] (0,-0.5) -- (0,2.5) node[above] {$z$};
  \node[below left] at (0,0) {$O$};
  
  \draw[domain=0:1.414, samples=100, thick] plot (\x, {2 - \x*\x});
  \draw[domain=-1.414:0, samples=100, dashed] plot (\x, {2 - \x*\x});
  
  \coordinate (P) at (1,1);
  \fill (P) circle (1.5pt) node[above right] {$P$};
  
  \draw[thick] (0,3) -- (1.5,0);
  \node[left] at (0,2) {$2$};
  \node[below] at (1.5,0) {$\frac{3}{2}$};
  \node[below] at (1,0) {$1$};
  \node[left] at (0,1) {$1$};
\end{tikzpicture}
\qquad
\begin{tikzpicture}[scale=1.2, >=stealth]
  \draw[->] (0,0) -- (2,0) node[right] {$x$};
  \draw[->] (0,0) -- (0,2) node[above] {$y$};
  \node[below left] at (0,0) {$O$};
  
  \draw (0,0) -- (60:1.8) node[above right] {$C$};
  \draw (0,0) -- (1.8,0);
  \draw (1.8,0) arc (0:60:1.8);
  
  \draw (0.5,0) arc (0:60:0.5);
  \node at (30:0.7) {$\theta$};
  \node at (30:1.3) {$r$};
  \node at (0.9,-0.2) {$\frac{3}{2}$};
\end{tikzpicture}
\end{center}

\subsection*{第6問}

[解] $OB = OC = OD = 4$ より, $B, C, D$ は, $O$ を中心とする半径4の球面 $S$ 上に存在す. まず, $\triangle BCD$ を固定して, $A$ をとる時, 明らかに $OA \perp \triangle BCD$ の法線ベクトルと等しく, かつ $\triangle BCD$ と $A$ が $O$ に関して反対側にある時, 四面体 $ABCD$ の体積は最大 $\dots \text{①}$.

以下 $\triangle BCD$ について, $O$ から下ろした垂線 $H$, $OH = h$ とすると, 四面体 $ABCD$ の体積 $V$ の最大値は, ($0 \le h < 4$)
\[
V = \frac{1}{3} \triangle BCD \cdot AH \quad \dots \text{①}'
\]
で与えられる ($\because \text{①}$). $AH = 4+h$ で一定だから, 次に $\triangle BCD$ の面積の最大値をかんがえる. $HB = HC = HD = \sqrt{16-h^2} \equiv t$ とおく.

\begin{center}
\begin{tikzpicture}[scale=1.2, >=stealth]
  \coordinate (O) at (0,0);
  \coordinate (A) at (0,1.5);
  \coordinate (H) at (0,-0.8);
  \coordinate (B) at (-1.2,-1.2);
  \coordinate (C) at (1.2,-1.2);
  \coordinate (D) at (0.4,-0.5);
  
  \draw[dashed] (O) circle (1.5);
  \draw (A) -- (B) -- (C) -- cycle;
  \draw (A) -- (D);
  \draw (B) -- (D) -- (C);
  \draw[dashed] (A) -- (H);
  
  \node[above] at (A) {$A'$};
  \node[above left] at (A) {$A$};
  \node[right] at (O) {$O$};
  \node[below] at (H) {$H$};
  \node[left] at (B) {$B$};
  \node[right] at (C) {$C$};
  \node[above right] at (D) {$D$};
  \node[right] at (0,0.7) {$S$};
  \node[right] at (0,-0.4) {$h$};
\end{tikzpicture}
\qquad
\begin{tikzpicture}[scale=1.2, >=stealth]
  \coordinate (H) at (0,0);
  \coordinate (B) at (0,1.5);
  \coordinate (C) at (-1.3,-0.75);
  \coordinate (D) at (1.3,-0.75);
  \coordinate (H') at (0,-0.75);
  
  \draw (B) -- (C) -- (D) -- cycle;
  \draw (B) -- (H');
  \draw[dashed] (H) circle (1.5);
  
  \node[above] at (B) {$B$};
  \node[left] at (C) {$C$};
  \node[right] at (D) {$D$};
  \node[above right] at (H) {$H$};
  \node[below] at (H') {$H'$};
  
  \node[left] at (0.8,0.75) {$t$};
  \node[left] at (-0.65,0.375) {$t$};
  \node at (0.2,-0.5) {$\theta$};
\end{tikzpicture}
\end{center}

$CD$ を固定すると, $B$ は $O$ を中心とする半径 $t$ の円周上をうごくので, $\triangle BCD$ の最大値を与えるのは $BH \perp CD$ かつ $CD$ と $B$ が $H$ に関して反対にある時. つまり $\triangle BCD$ が $BC=BD$ の二等辺三角形の時. この時 $\angle HCD = \theta$ とおく ($0 < \theta < \pi/2$).
\begin{align*}
\triangle BCD &= \frac{1}{2} CD \cdot BH' = \frac{1}{2} \cdot (2 t \cos\theta) \cdot (t + t \sin\theta) \\
&= t^2 (1 + \sin\theta) \cos\theta \equiv f(\theta) \\
f'(\theta) &= t^2 (-2\sin^2\theta - \sin\theta + 1) \quad (s = \sin\theta) \\
&= t^2 (-2s+1)(s+1)
\end{align*}
よって $t^2 > 0, 0 < \theta < \pi/2$ より, $f(\theta)$ は $\theta = \pi/6$ で最大値 $\frac{3\sqrt{3}}{4} t^2$ をとる.
この時, $\triangle BCD$ は正三角形 $\dots \text{②}$.

以上より, $h$ が一定の時の $V$ の最大値は, ($\because \text{①②}$)
\begin{align*}
V &= \frac{1}{3} \cdot \frac{3\sqrt{3}}{4} (16-h^2) (4+h) \\
&= \frac{\sqrt{3}}{4} (16-h^2) (4+h)
\end{align*}
で与えられる. $f(h) = (4+h)(16-h^2)$ とおくと, $f(h)$ が最大の時 $V$ も最大で, $f'(h) = -(3h+8)(h-2)$ より, $f(h)$ は $0 \le h < 4$ で, $h=2$ で最大.

以上より, もとめる最大値は
\[
V = \frac{\sqrt{3}}{4} \cdot 12 \cdot 3 = 9\sqrt{3}
\]
である.

\end{document}
