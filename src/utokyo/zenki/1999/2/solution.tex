\documentclass[../../../../main.tex]{subfiles}
\begin{document}

\begin{flushright}
\footnotesize\textit{【自動文字起こし・要確認】}
\end{flushright}

\noindent
\textbf{[解]} $\alpha = \frac{-1}{2+4i}$ とすると、$\alpha$ は $\alpha = (3+4i)\alpha + 1$ をみたすので、
\[
z_{n+1} - \alpha = (3+4i)(z_n - \alpha)
\]
$z_1 = 1$ とあわせて、くり返し用いると
\[
z_n = (3+4i)^{n-1}(1 - \alpha) + \alpha = \frac{1}{2+4i}(3+4i)^{n-1} - \frac{1}{2+4i} = \frac{1-2i}{10}\{(3+4i)^{n-1} - 1\} \quad \cdots \textcircled{1}
\]
である。

\bigskip

\noindent
(1) 与式の各辺 $0$ 以上だから、2乗して
\[
\frac{9}{16} \cdot 5^{2n-2} < |z_n|^2 < \frac{1}{16} \cdot 5^{2n} \quad \cdots \textcircled{2}
\]
又、\textcircled{1}から
\[
|z_n| = \left|\frac{1-2i}{10}\right| |(3+4i)^{n-1} - 1|
\]
で、$\left|\frac{1-2i}{10}\right| = \frac{\sqrt{5}}{10}$ から、三角不等式より
\[
\begin{cases} 
|z_n| \le \frac{\sqrt{5}}{10} \{ |3+4i|^{n-1} + |-1| \} = \frac{\sqrt{5}}{10} (5^{n-1} + 1) \\ 
|z_n| \ge \frac{\sqrt{5}}{10} \{ |3+4i|^{n-1} - |-1| \} = \frac{\sqrt{5}}{10} (5^{n-1} - 1) 
\end{cases}
\]
だから、$A = 5^n$ として、
\[
\frac{1}{20}(A-1)^2 \le |z_n|^2 \le \frac{1}{20}(A+1)^2 \quad \cdots \textcircled{3}
\]
である。まず、\textcircled{2}の右側について、$A>0$ より
\[
\frac{1}{20}(A+1)^2 < \frac{1}{16} A^2 \iff A^2 > 4(2A+1) \iff A > 8 + \frac{4}{A} \quad \cdots \textcircled{4}
\]
であり、$n \ge 2$ の時、$A = 5^n \ge 25 > 8 + \frac{4}{25} \ge 8 + \frac{4}{A}$ から \textcircled{4} は成立。

\bigskip

\noindent
次に \textcircled{2}の左側について、
\begin{align*}
\frac{1}{20}(A-1)^2 > \frac{9}{25 \cdot 16} A^2 &\iff 11A^2 - 40A + 20 > 0 \\
&\iff \left( A - \frac{20 + 6\sqrt{5}}{11} \right) \left( A + \frac{20 - 6\sqrt{5}}{11} \right) > 0 \quad \cdots \textcircled{5}
\end{align*}
であり、$A \ge 25 > \frac{20 + 6\sqrt{5}}{11} > \frac{20 - 6\sqrt{5}}{11}$ から \textcircled{5} は常に成立。

\bigskip

\noindent
以上 \textcircled{4} 及び \textcircled{5} から、$n \ge 2$ の時、
\[
\frac{9}{16} \cdot 5^{2n-2} < \frac{1}{20}(A-1)^2 \le |z_n|^2 \le \frac{1}{20}(A+1)^2 < \frac{1}{16} \cdot 5^{2n}
\]
で、両辺平方根をとって
\[
\frac{3}{4} \cdot 5^{n-1} < |z_n| < \frac{1}{4} \cdot 5^n
\]
又、$n=1$ の時、$|z_1|=1$ から問題意は成立するから、以上より不等式は成立する。 \hfill $\qed$

\bigskip

\noindent
(2) (1)から数列 $\{|z_n|\}$ は単調増加である。($\because \frac{1}{4} \cdot 5^{n-1} < \frac{3}{4} \cdot 5^n$) \\
したがって、$r = \frac{5^n}{4}$ の時 $f(r) = n$ であり、$\frac{5^{n-1}}{4} < r \le \frac{5^n}{4}$ に対して、$n-1 \le f(r) \le n$ が成立する。又この時
\[
(n-1)\log 5 - \log 4 < \log r \le n\log 5 - \log 4
\]
が成立する。($\because \log r$ は単調増加) 以上の2式から、$n$ が十分大きい時
\[
\frac{n-1}{n\log 5 - \log 4} < \frac{f(r)}{\log r} < \frac{n}{(n-1)\log 5 - \log 4}
\]
が成立する。$r \to \infty$ の時 $n \to \infty$ で、この両辺は共に $\frac{1}{\log 5}$ に収束するから、はさみうちより
\[
\frac{f(r)}{\log r} \longrightarrow \frac{1}{\log 5} \hfill \qed
\]

\newpage

\end{document}
