\documentclass[../../../../main.tex]{subfiles}
\begin{document}

$xy$ 平面上の曲線 $y=\sin x$ に沿って，
図のように左から右へすすむ動点 $P$ がある．
$P$ の速さが一定 $V$ $(V>0)$ であるとき，
$P$ の加速度ベクトル $\vec{\alpha}$ の大きさの最大値を求めよ．
ただし，$P$ の速さとは $P$ の速度ベクトル $\vec{v}=(v_1,v_2)$ の大きさであり，
また $t$ を時間として $\displaystyle \vec{\alpha}= \left( \frac{dv_1}{dt},\frac{dv_2}{dt} \right)$ である．

\begin{figure}[htbp]
  \centering
  \begin{tikzpicture}[scale=1.2, >=stealth]
    \draw[->] (-0.8,0) -- (6.8,0) node[right] {$x$};
    \draw[->] (0,-1.5) -- (0,1.8) node[above] {$y$};
    \node[below left] at (0,0) {$O$};

    \draw[thick, domain=-0.8:6.5, samples=100] plot (\x, {sin(\x r)});

    \coordinate (P) at (2.0, {sin(2.0 r)});
    \fill (P) circle (1.5pt) node[above right] {$P$};

    \draw[->] (0.4, 0.6) arc (130:90:0.6);
    \draw[->] (2.2, 0.7) arc (60:20:0.6);
  \end{tikzpicture}
\end{figure}

\end{document}
