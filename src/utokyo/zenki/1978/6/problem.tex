\documentclass[../../../../main.tex]{subfiles}
\begin{document}

$xy$ 平面において放物線 $y=x^2$ の，$0<x<1$ に対応する部分を $L$ とする．
(すなわち $L= \{ (x,x^2) \mid 0<x<1 \}$ である．)
点 $P(x,x^2)$ における $L$ の接線が直線 $y=0$，直線 $x=1$ と交わる点をそれぞれ $A$，$B$ とする．
また座標が $(0,0)$，$(1,0)$，$(1,1)$ である三点を，
それぞれ $O$，$C$，$D$ とする．

以下つねに $0<x<1$ という範囲で考えるものとする．

\begin{figure}[htbp]
  \centering
  \begin{tikzpicture}[scale=2, >=stealth]
    \coordinate (O) at (0,0);
    \coordinate (C) at (2,0);
    \coordinate (D) at (2,2.5);
    \coordinate (B) at (2,1.6);
    \coordinate (A) at (0.4, 0);

    \draw[thick, domain=0:2, samples=100] plot (\x, {0.625*\x*\x});

    \draw[thick] (O) -- (C) -- (D) -- cycle;

    \coordinate (P) at (0.9, 0.506);
    \draw[thick] (A) -- (B);

    \draw[thick] (P) -- (0.9, 0.1) -- (1.3, 0.4) -- cycle;

    \fill (O) circle (1pt) node[below left] {$O$};
    \fill (A) circle (1pt) node[below] {$A$};
    \fill (C) circle (1pt) node[below right] {$C$};
    \fill (B) circle (1pt) node[right] {$B$};
    \fill (D) circle (1pt) node[above right] {$D$};
    \fill (P) circle (1pt) node[above left] {$P$};
    \node at (1.7, 1.7) {$M$};
  \end{tikzpicture}
\end{figure}

\begin{enumerate}
  \item $\triangle PAC$，$\triangle PCB$ の面積をそれぞれ $g(x)$，$h(x)$ とするとき，
  $g(x) \leqq h(x)$ となる $x$ の範囲を求めよ．

  \item 線分 $OC$ および線分 $CD$ と放物線の一部 $L$ で囲まれた範囲を $M$ とする．
  ただし $M$ はその周である線分 $OC$，$CD$ および $L$ を含むものとする．
  いま $L$ 上の点 $P(x,x^2)$ を頂点とし，$M$ に含まれるような三角形のうちで，
  最大の面積を持つものの面積を $f(x)$ とする．
  関数 $f(x)$ を求め，そのグラフを描け．
  また $f(x)$ の極値を求めよ．
  ただし $I=\{ x \mid 0<x<1 \}$ の点 $a$ で，関数 $f$ が極小値(または極大値)をとるとは，
  $a$ に近い $I$ のすべての点 $x$ に対して $f(a) \leqq f(x)$ (または $f(a) \geqq f(x)$) となることをいう．
  極大値と極小値をあわせて極値という．
\end{enumerate}

\end{document}
