\documentclass[../../main.tex]{subfiles}
\begin{document}

{\bf[解]}
$\cos x=t$,$\sin x=s$とおくと
\begin{align}
  f'(x)=at-bs+2c\cos 2x
\end{align}
である．まず，極大値の条件から
$f\left(\dfrac{\pi}{4}\right)=0$が必要である．これと題意の条件から
\begin{align}
  &\left\{
    \begin{array}{l}
      f'\left(\dfrac{\pi}{4}\right)=0 \\
      f\left(\dfrac{\pi}{4}\right)=6\sqrt{2} \\
      \int_{0}{2\pi}f(x)\cos x dx=5\pi
    \end{array}
    \right.  \\
    \Leftrightarrow
    &\left\{
      \begin{array}{l}
        \dfrac{\sqrt{2}}{2}(a-b)=0 \\
        \dfrac{\sqrt{2}}{2}(a+b)+c=6\sqrt{2} \\
        b\pi=5\pi
      \end{array}
      \right.
    \end{align}
    故に$(a,b,c)=(5,5,\sqrt{2})$となる．
    $f'(x)$に値を代入する．
    \begin{align}
      f'(x)&=5t-5s+2\sqrt{2}(t^2-s^2) \\
      &=(t-s)\left(5+4\sin\left(x+\dfrac{\pi}{4}\right)\right)
    \end{align}
    したがって，下表を得る．

    \begin{table}[htb]
      \centering
      \caption{$f(x)$ の増減表}
      \begin{tabular}{c|ccccccc}
        $x$ & $0$ & $\cdots$ & $\dfrac{\pi}{4}$ & $\cdots$ & $\dfrac{5\pi}{4}$ & $\cdots$ & $2\pi$ \\
        \hline
        $f'(x)$ & & $+$ & $0$ & $-$ & $0$ & $+$ & \\
        \hline
        $f(x)$ & $5$ & $\nearrow$ & $6\sqrt{2}$ & $\searrow$ & $-4\sqrt{2}$ & $\nearrow$ & $5$
      \end{tabular}
    \end{table}

    故に$x=\dfrac{\pi}{4}$で極大となり十分である．以上から
    \begin{enumerate}[(1)]
      \item $(a,b,c)=(5,5,\sqrt{2})$
      \item $\min f(x)=f\left(\dfrac{5\pi}{4}\right)=-4\sqrt{2}$
    \end{enumerate}
    となる．$\cdots$(答)
    \newpage
    \end{document}
