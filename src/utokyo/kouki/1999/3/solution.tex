\documentclass[../../../../main.tex]{subfiles}
% \documentclass[a4paper,10pt]{ltjsarticle}
\begin{document}
\rhead{東京大学後期$1999$年$3$番}
\setcounter{equation}{0}
\begin{oframed}
  \input{problem.tex}
\end{oframed}
\setlength{\columnseprule}{0.4pt}
\begin{multicols}{2}
  {\bf[解]}

  (1)

  % \begin{figure}[H]
  %   \begin{minipage}{0.48\linewidth}
  %     \centering
  %     \textbf{1° $n>0$ の時}
  %     \begin{tikzpicture}[scale=1.2, every node/.style={font=\small}]
  %       % Polygon n
  %       \coordinate[label=above left:$A_n$] (An) at (0,1.8);
  %       \coordinate[label=below left:$B_n$] (Bn) at (0.2,0);
  %       \coordinate[label={[yshift=-0.5em]below right:$C_n=B_{n+1}$}] (Cn) at (2.2,0.2);
  %       \coordinate[label={[yshift=0.5em]above right:$D_n=A_{n+1}$}] (Dn) at (1.8,1.8);

  %       \draw (An) -- (Bn) -- (Cn) -- (Dn) -- cycle;

  %       % Points for n+1
  %       \coordinate[label=above right:$D_{n+1}$] (Dn1) at (2.8,1.5);
  %       \coordinate[label=below right:$C_{n+1}$] (Cn1) at (2.5, 0.8);

  %       \draw (Dn) -- (Dn1);
  %       \draw (Dn) -- (Cn1);
  %     \end{tikzpicture}
  %   \end{minipage}
  %   \hfill
  %   \begin{minipage}{0.48\linewidth}
  %     \centering
  %     \textbf{2° $n<0$ の時}

  %     \begin{tikzpicture}[scale=1.1, every node/.style={font=\small}]
  %       % Polygon
  %       \coordinate[label=above right:$B_n$] (Bn) at (2.5,2.2);
  %       \coordinate[label=right:$A_n$] (An) at (2.8,1);
  %       \coordinate[label={[yshift=-0.7em]below:$D_n=A_{n+1}$}] (Dn) at (1.5,0);
  %       \coordinate[label=left:$D_{n+1}$] (Dn1) at (-0.5,1.2);
  %       \coordinate[label=above:$C_{n+1}$] (Cn1) at (0.5,2.5);
  %       \coordinate[label=above left:$C_n=B_{n+1}$] (Cn) at (1.2,2.2);

  %       \node at (0.8, 1.2) {$\Rightarrow$};

  %       \draw (Bn) -- (An) -- (Dn) -- (Dn1) -- (Cn1) -- (Cn) -- (Bn);
  %     \end{tikzpicture}
  %   \end{minipage}
  % \end{figure}


  % \begin{tikzpicture}[scale=0.45]
  %         % Axes
  %         \draw[->] (-1,0) -- (10,0) node[below left] {$x$};
  %         \draw[->] (0,-1) -- (0,14) node[below left] {$y$};

  %         % Ticks
  %         \foreach \x in {2,4,8}
  %             \draw (\x,0.2) -- (\x,-0.2) node[below] {\x};
  %         \foreach \y in {4,12}
  %             \draw (0.2,\y) -- (-0.2,\y) node[left] {\y};

  %         % Points
  %         \coordinate[label=below right:$A_0$] (A0) at (2,1);
  %         \coordinate[label=right:$B_0$] (B0) at (8,4);
  %         \coordinate[label=above:$C_0$] (C0) at (3,8);
  %         \coordinate[label=left:$D_0$] (D0) at (2,2);
  %         \coordinate[label=above right:$P$] (P) at (4,12);

  %         % Polygon
  %         \draw (A0) -- (B0) -- (C0) -- (D0) -- cycle;

  %         % Dashed lines for P
  %         \draw[dashed] (P) -- (4,0);
  %         \draw[dashed] (P) -- (0,12);

  %         % Dots for points
  %         \foreach \pt in {A0, B0, C0, D0, P}
  %             \fill (\pt) circle (3pt);
  %     \end{tikzpicture}

  \begin{figure}[H]
    \centering
    \begin{tikzpicture}
      \begin{axis}[
          axis lines=none, % 軸、目盛り、ラベルをすべて非表示にする
          clip=false,      % 描画が軸範囲外にはみ出るのを許可
          xmin=-3, xmax=5,
          ymin=-2, ymax=5,
        ]

        % --- 頂点の座標を定義 ---
        % 共有する頂点 (D_n = A_{n+1} と C_n = B_{n+1})
        \coordinate (Dn_An1) at (axis cs:0, 0);
        \coordinate (Cn_Bn1) at (axis cs:-0.5, 4);

        % 四角形 n の残りの頂点
        \coordinate (An) at (axis cs:4, 1.5);
        \coordinate (Bn) at (axis cs:2, 5);

        % 四角形 n+1 の残りの頂点
        \coordinate (Cn1) at (axis cs:-3, 3.5);
        \coordinate (Dn1) at (axis cs:-3, 0);


        % --- 四角形 n (右側) の描画 ---
        \draw[thick] (An) -- (Bn) -- (Cn_Bn1) -- (Dn_An1) -- cycle;

        % --- 四角形 n+1 (左側) の描画 ---
        \draw[thick] (Dn_An1) -- (Cn_Bn1) -- (Cn1) -- (Dn1) -- cycle;


        % --- 各頂点のラベルを追加 ---
        \node[above right] at (An) {$A_n$};
        \node[below right] at (Bn) {$B_n$};
        \node[above] at (Cn1) {$C_{n+1}$};
        \node[below] at (Dn1) {$D_{n+1}$};

        % 共有している頂点のラベル
        \node[below] at (Dn_An1) {$D_n=A_{n+1}$};
        \node[above] at (Cn_Bn1) {$C_n=B_{n+1}$};

      \end{axis}
    \end{tikzpicture}
    \caption{四角形$A_nB_nC_nD_n$と$A_{n+1}B_{n+1}C_{n+1}D_{n+1}$の様子}
    \label{1999-3:fig:0}
  \end{figure}

  \begin{figure}[H]
    \centering

    \begin{tikzpicture}
      \def\t{0.7}
      \begin{axis}[
          axis lines=middle,      % 軸を左と下に表示
          xlabel=$x$,           % x軸ラベル
          ylabel=$y$,           % y軸ラベル
          xmin=0, xmax=9,       % x軸の範囲
          ymin=0, ymax=14,      % y軸の範囲
          xtick={2, 4, 8},      % x軸の目盛り
          ytick={1, 4, 12},     % y軸の目盛り
          % enlarge x limits={abs=0.5}, % x軸の余白
          % enlarge y limits={abs=0.5}, % y軸の余白
          % tick style={color=black},
          % label style={anchor=near axis},
          xticklabel style={anchor=north},
          yticklabel style={anchor=east},
        ]

        % --- 点の座標を定義 ---
        % 固定点
        \coordinate (A0) at (axis cs:2,1);
        \coordinate (B0) at (axis cs:8,4);
        \coordinate (P)  at (axis cs:4,12);
        % パラメータtに依存する点
        \coordinate (C0) at (axis cs:{4*\t}, {12*\t});
        \coordinate (D0) at (axis cs:{\t}, {3*\t});

        % --- 四角形 A0B0C0D0 の描画 ---
        % fillで内部を薄く塗りつぶし、見やすくしています
        \draw[thick, blue, fill=blue, fill opacity=0.1] (A0) -- (B0) -- (C0) -- (D0) -- cycle;
        \draw[thick, blue, ->, fill=blue, fill opacity=0.1] (A0) -- ($(A0)!0.5!(D0)$);
        \draw[thick, blue, ->, fill=blue, fill opacity=0.1] (B0) -- ($(B0)!0.5!(C0)$);

        % --- 各点のプロットとラベルの表示 ---
        \fill (A0) circle (2pt) node[below right] {$A_0$};
        \fill (B0) circle (2pt) node[above right] {$B_0$};
        \fill (P)  circle (2pt) node[above right] {$P$};
        \fill (C0) circle (2pt) node[above left]  {$C_0$};
        \fill (D0) circle (2pt) node[above left]  {$D_0$};

        % --- 補助線の描画 (手書きの図に合わせて) ---
        % 各点から軸への破線
        \draw[dashed, gray] (axis cs:0,12) -| (P);
        \draw[dashed, gray] (axis cs:4,0)  -- (P);
        \draw[dashed, gray] (axis cs:0,4) -| (B0);
        \draw[dashed, gray] (axis cs:8,0) -- (B0);
        \draw[dashed, gray] (axis cs:0,1) -| (A0);
        \draw[dashed, gray] (axis cs:2,0) -- (A0);

        % C0 と D0 が乗る直線の描画（点線）
        \draw[dotted, gray, thick] (axis cs:0,0) -- (P); % 直線OP
        \draw[dotted, gray, thick] (axis cs:0,0) -- (axis cs:1.5,4.5); % 直線OD0の方向

      \end{axis}
    \end{tikzpicture}
    \caption{四角形$A_0B_0C_0D_0$の様子}
    \label{1999-3:fig:1}
  \end{figure}


  与えられた座標の情報を用いて，$\triangle B_0OP$に余弦定理を適用すると
  \begin{align*}
    \cos \angle B_0OP
     & = \frac{\overrightarrow{OB_0} \cdot \overrightarrow{OP}}{|\overrightarrow{OB_0}| |\overrightarrow{OP}|} \\
     & = \frac{160}{\sqrt{80} \cdot \sqrt{160}}                                                                \\
     & = \frac{\sqrt{2}}{2}
  \end{align*}
  である．
  点$B_0,P$はいずれも第一章限にあって $0 < \angle B_0OP < \pi/2$ だから，
  \begin{align*}
    \angle B_0OP = \frac{\pi}{4}
  \end{align*}
  である．$\cdots$(答)

  \vspace{10pt}
  (2)
  点$C_0$が線分$OP$上にあるから，$C_0(4t, 12t) \ (t \in \mathbb{R}, 0< t < 1)$ とおける．

  この時$\triangle OA_0D_0$と$\triangle OB_0C_0$ が$1:4$の相似であることから，
  $D_0(t, 3t)$ とおける．

  すると，四角形$A_0B_0C_0D_0$の辺の長さについて
  \begin{align*}
    \overline{A_0B_0} & = 3\sqrt{5}     \\
    \overline{C_0D_0} & = 3\sqrt{10}|t|
  \end{align*}
  となる．

  $K_n$の定義から，$K_n$と$K_{n+1}$の相似比は$\overline{A_0B_0}:\overline{C_0D_0}$に等しく
  \begin{align}
    K_n: K_{n+1}
     & =1:\frac{3\sqrt{10}t}{3\sqrt{5}}  \\
     & = 1:\sqrt{2}t \label{1999-3:eq:1}
  \end{align}
  である．

  題意のように$K_n=K_0$なる$n$があるとき，まずはこれらの大きさが等しいことが必要であって，
  相似比が$1:1$であることが必要だから
  \begin{align*}
     & \sqrt{2}t = 1          \\
    \therefore
     & t = \frac{\sqrt{2}}{2}
  \end{align*}
  である．よってこの時
  \begin{align*}
    C_0(2\sqrt{2},6\sqrt{2})
  \end{align*}
  となる．

  逆にこの時任意の$K_n$が合同だから
  \begin{align*}
    \angle A_0OD_n = \angle B_0OP = \pi/4 \quad \cdots\cdots②
  \end{align*}
  が成り立ち，$x$軸と線分$OB_0$のなす角度$\alpha$として
  \begin{align*}
    A_n(\sqrt{3} \cos(\alpha + \pi n/4), \sqrt{3} \sin(\alpha + \pi n/4))
  \end{align*}
  と書ける．
  $A_n=A_0$の時が$K_n=K_0$となる時だから，求める$n$の条件は
  \begin{align*}
     & n\frac{\pi}{4} = 2k\pi & (k \in \mathbb{Z}) \\
     & n \equiv 0             & (\pmod{8})
  \end{align*}
  である．$\cdots$(答)

  \vspace{10pt}

  (3)
  題意の条件より，任意の$n$に対して$\triangle OC_k B_k$ が相似である．
  従って，任意の$n$に対して$\angle A_nOD_n$が等しく，(2)と同様
  \begin{align*}
    \angle A_0OD_n = \angle B_0OP = \pi/4
  \end{align*}
  が成り立つ．

  従って，$A_n$の座標は，実数列$a_n$を用いて
  \begin{align}
     & A_n(a_n \cos(\alpha + \pi n/4), a_n \sin(\alpha + \pi n/4)) \label{1999-3:eq:3} \\
     & a_0 = \sqrt{5}
  \end{align}
  と書ける．
  (2)で求めた相似比\cref{1999-3:eq:1}より
  \begin{align*}
     & \overrightarrow{OA_{n+1}}|: |\overrightarrow{OA_n}| = \sqrt{2}t : 1 \\
    \therefore
     & a_{n+1} = \sqrt{2}t a_{n}
  \end{align*}
  と書けるから，$a_0=\sqrt{5}$のもとでこの漸化式を解いて
  \begin{align}
    a_n = \sqrt{5}\left(\sqrt{2}t\right)^{n} \label{1999-3:eq:2}
  \end{align}
  と求まる．
  以上より，$A_n$は$\mod 8$で同じ$n$のものをグループとして点Oから始まる異なる半直線上にならぶ．

  題意のように四角形が平面を敷き詰める時，
  半直線$OB_0$, $OP$にはさまれた領域に\cref{1999-3:fig:3}のように
  $K_n$が詰まることが従って必要となる．

  \begin{figure}[H]
    \centering
    \begin{tikzpicture}[
        scale=0.5, % 図全体のスケールを調整
        dot/.style={fill, circle, inner sep=1.5pt} % 点のスタイルを定義
      ]
      % 原点Oのラベル
      \node[below left] at (0,0) {$O$};

      % y=3x の半直線を描画
      \draw[gray, thick] (0,0) -- (7, 21) node[above] {$P$};

      % y=x/2 の半直線を描画
      \draw[gray, thick] (0,0) -- (14, 7) node[right] {$B_0$};

      % t=1から6までループ
      \foreach \t in {1, 2, ..., 6}
        {
          % 点(t, 3t)と点(2t, t)を定義
          \coordinate (P) at (\t, 3*\t);
          \coordinate (B) at (2*\t, \t);

          % 2点を結ぶ線分を描画
          \draw (P) -- (B);

          % 2点にドットを打つ
          \node[dot] at (P) {};
          \node[dot] at (B) {};
        }
    \end{tikzpicture}
    \caption{半直線$OB_0$, $OP$にはさまれた領域に$K_n$がしき詰まっている様子．}
    \label{1999-3:fig:3}
  \end{figure}


  図でとなりあう$K_n$の相似比は
  \begin{align*}
    \overline{A_0P_0} : \overline{B_0C_0} = 1:4
  \end{align*}
  である．
  $a_{n+8}:a_{n}$の相似比がこれに等しいことが必要である．
  \cref{1999-3:eq:2}より，この条件は
  \begin{align*}
     & \left(\sqrt{2}t\right)^{8} = 4, \frac{1}{4}       \\
    \therefore
     & \sqrt{2}t=4^{1/8}, \left(\frac{1}{4}\right)^{1/8} \\
    \therefore
     & t=2^{-1/4}, 2^{-3/4}
  \end{align*}
  となる．
  この時対応する$C_0$の座標は
  \begin{align*}
    C_0(2^{7/4}, 3 \cdot 2^{7/4}) \\
    C_0(2^{5/4}, 3 \cdot 2^{5/4})
  \end{align*}
  である．
  逆にこの時，$K_n$は図のように平面を敷き詰めるから十分である．
  $\cdots$(答)
  \vspace{10pt}

  \begin{figure}[H]
    \centering
    \begin{tikzpicture}

      % --- パラメータ定義 ---
      \def\t{0.75}      % 渦の広がり具合を調整 (t > 1/sqrt(2) で拡大)
      \def\alpha{atan(1/2)}   % 開始角度（度数法）
      % --------------------

      \begin{axis}[
          axis equal,      % 縦横比を1:1に
          axis lines=middle, % 軸、目盛り、ラベルをすべて非表示
          xmin=-10, xmax=10,
          ymin=-10, ymax=10,
          xlabel=$x$,
          ylabel=$y$,
          xtick={\empty},
          ytick={\empty},
          clip=false,
          % --- 座標計算用の関数を定義 ---
          declare function={
              r(\n) = sqrt(5) * pow(sqrt(2)*0.75, \n));
              xcoord(\n) = r(\n) * cos(26.56 + \n*45);
              ycoord(\n) = r(\n) * sin(26.56 + \n*45);
            }
        ]

        % --- 中心の放射状の直線を描画 ---
        \pgfplotsinvokeforeach{0,1,2,3,4,5,6,7} {
          \edef\temp{% see https://tikz.dev/pgfplots/utility-commands#autosec-12770
            \noexpand\draw[gray, thin] (0,0) -- (26.56+#1*45:10); % alpha=26.56
          } \temp
        }

        \coordinate (Q) at (axis cs:6,3);
        \node[above right, font=\small] at (Q) {$Q$};
        \fill (Q) circle (1.5pt);

        % % --- 特定の点とラベルを関数を使って直接プロット ---
        % % A_0
        % \coordinate (A8) at (axis cs:{sqrt(5) * (sqrt(2)*\t)^{8} * cos(\alpha+8*45)},{sqrt(5) * (sqrt(2)*\t)^{8} * sin(\alpha+8*45)});
        % \node[above right, font=\small] at (A8) {$A_0$};
        % \fill (A8) circle (1.5pt);

        % \node[above right, font=\small] at (axis cs:{xcoord(1)}, {ycoord(1)}) {$A_0$};
        % \fill (axis cs:{xcoord(1)}, {ycoord(1)}) circle (1.5pt);

        \pgfmathsetmacro{\rzero}{sqrt(5) * (sqrt(2)*\t)^0}
        \pgfmathsetmacro{\xzero}{\rzero * cos(\alpha + 0*45)}
        \pgfmathsetmacro{\yzero}{\rzero * sin(\alpha + 0*45)}
        % A_0 のラベルを追加
        \node[below, font=\footnotesize] at (axis cs:\xzero, \yzero) {$A_0$};
        \fill (axis cs:\xzero, \yzero) circle (1.5pt);

        % --- 渦を描画 ---
        % 最初の点 A_0 の座標を計算
        \pgfmathsetmacro{\rprev}{sqrt(5) * (sqrt(2)*\t)^0}
        \pgfmathsetmacro{\xprev}{\rprev * cos(\alpha + 0*45)}
        \pgfmathsetmacro{\yprev}{\rprev * sin(\alpha + 0*45)}


        % https://tex.stackexchange.com/questions/170664/foreach-not-behaving-in-axis-environment
        % n=1 からループを開始して、前の点と結んでいく
        \pgfplotsinvokeforeach{1,...,28} {
          % 現在の点 A_n の半径と座標を計算
          \pgfmathsetmacro{\rcurr}{sqrt(5) * (sqrt(2)*\t)^#1}
          \pgfmathsetmacro{\xcurr}{\rcurr * cos(\alpha + #1*45)}
          \pgfmathsetmacro{\ycurr}{\rcurr * sin(\alpha + #1*45)}

          % https://tex.stackexchange.com/questions/746744/pgfplotsinvokeforeach-with-pgfmathsetmacro-doesnt-work
          \edef\temp{% see https://tikz.dev/pgfplots/utility-commands#autosec-12770
            \noexpand\draw[thick, blue] (axis cs:\xprev, \yprev) -- (axis cs:\xcurr, \ycurr);
          } \temp
          % 前の点(A_{n-1})から現在の点(A_n)へ線分を引く

          % 現在の座標を「前の座標」として保存
          \pgfmathsetmacro{\xprev}{\xcurr}
          \pgfmathsetmacro{\yprev}{\ycurr}
        }

        % % --- 特定の点とラベルを関数を使って直接プロット ---
        % % A_8
        \coordinate (Aeight) at (axis cs:{sqrt(5) * pow(sqrt(2)*\t, 8) * cos(\alpha + 8*45)},{sqrt(5) * pow(sqrt(2)*\t, 8) * sin(\alpha + 8*45)});
        \node[below right, font=\footnotesize] at (Aeight) {$A_8$};
        \fill (Aeight) circle (1.5pt);

        % % A_8        
        % \node[above right, font=\small] at (axis cs:{xcoord(8)}, {ycoord(8)}) {$A_8$};
        % \fill (axis cs:{xcoord(8)}, {ycoord(8)}) circle (1.5pt);

        % % A_16
        \coordinate (Afifteen) at (axis cs:{sqrt(5) * pow(sqrt(2)*\t, 16) * cos(\alpha + 16*45)},{sqrt(5) * pow(sqrt(2)*\t, 16) * sin(\alpha + 16*45)});
        \node[below right, font=\footnotesize] at (Afifteen) {$A_{16}$};
        \fill (Afifteen) circle (1.5pt);

        % % A_24
        \coordinate (A24) at (axis cs:{sqrt(5) * pow(sqrt(2)*\t, 24) * cos(\alpha + 24*45)},{sqrt(5) * pow(sqrt(2)*\t, 24) * sin(\alpha + 24*45)});
        \node[below right, font=\footnotesize] at (A24) {$A_{24}$};
        \fill (A24) circle (1.5pt);


        % \node[above right, font=\small] at (axis cs:{xcoord(24)}, {ycoord(24)}) {$A_{24}$};
        % \fill (axis cs:{xcoord(24)}, {ycoord(24)}) circle (1.5pt);

      \end{axis}
    \end{tikzpicture}
    \caption{$t=2^{-\frac{1}{4}}$ の時，$K_n$が$xy$平面にしき詰まっている様子．}
    \label{1999-3:fig:4}
  \end{figure}

  次に，点$Q(100,50)$について考える．
  この点は直線$OB_0$上にあって，また$|\overrightarrow{OQ}| = 50\sqrt{5}$ である．
  二つの$t$の値についてそれぞれ調べる．
  いずれの場合も，$A_n$が直線$OB_n$上に存在するのは\cref{1999-3:eq:2}より
  $n$が$8$の倍数の時である．従って，$A_{8n}$と$Q$の位置関係を調べればQを含む$K_n$がわかる．

  \vspace{5pt}
  \textbf{1° $t=2^{-\frac{1}{4}}$ の時}

  \cref{1999-3:eq:2}より
  \begin{align*}
    a_{8n} = \sqrt{5}\cdot 4^n
  \end{align*}
  は単調増加で，
  \begin{align*}
    |\overrightarrow{OA_{16}}| < 50\sqrt{5} < |\overrightarrow{OA_{24}}|
  \end{align*}
  である．
  従って求める$n$は図より$n=15,16$である．

  \vspace{5pt}
  \textbf{2° $t=2^{-\frac{3}{4}}$ の時}

  \cref{1999-3:eq:2}より
  \begin{align*}
    a_{8n} = \frac{\sqrt{5}}{4^n}
  \end{align*}
  は単調減少で，
  \begin{align*}
    |\overrightarrow{OA_{-16}}| < 50\sqrt{5} < |\overrightarrow{OA_{-24}}|
  \end{align*}
  である．
  従って求める$n$は図より$n=-16,-17$である．

  \vspace{1em}
  以上まとめて，求める$C_0$および$Q$を含む$K_n$は
  \begin{align*}
    \begin{dcases}
      C_0(2^{\frac{7}{4}}, 3 \cdot 2^{\frac{7}{4}}) \text{ の時、} n=15, 16 \\
      C_0(-2^{\frac{7}{4}}, -3 \cdot 2^{\frac{7}{4}}) \text{ の時、} n=-16, -17
    \end{dcases}
  \end{align*}
  である．$\cdots$(答)
  \vspace{10pt}
  {\bf[解説]}



  \newpage
\end{multicols}
\end{document}