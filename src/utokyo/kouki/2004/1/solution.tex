\documentclass[../../../../main.tex]{subfiles}
% \documentclass[a4paper,10pt]{ltjsarticle}
\usepackage{luatexja}
\usepackage[hiragino-pron]{luatexja-preset}

\usepackage[truedimen,top=25truemm,bottom=20truemm,left=15truemm,right=15truemm]{geometry}
\setlength{\textwidth}{54\zw}
\setlength{\textheight}{73\zw}

\usepackage{amsmath,amssymb,ascmac}
\usepackage{enumerate}
\usepackage{multicol}
\usepackage{cleveref}
\usepackage{framed}
\usepackage{fancyhdr}
\usepackage{latexsym}
\usepackage{tikz}
\usepackage{pgfplots}
 \usetikzlibrary{math}
% \usepackage{indent}
\usepackage{cases}
\usepackage{float}
\allowdisplaybreaks
\pagestyle{fancy}
\lhead{}
\chead{}
\begin{document}
\rhead{東京大学後期$2004$年$1$番}
\setcounter{equation}{0}
\begin{oframed}
    \input{../../../problems/2004/1/problem.tex}
\end{oframed}
\setlength{\columnseprule}{0.4pt}
\begin{multicols}{2}
    {\bf[解]}

    (1)

    \begin{figure}[H]
        \centering
        \begin{tikzpicture}
            \begin{axis}[
                    axis lines=middle,   % 軸を原点を通るように描画
                    xlabel=$x$,          % x軸ラベル
                    ylabel=$y$,          % y軸ラベル
                    xmin=0, xmax=1.2,    % x軸の範囲
                    ymin=-0.6, ymax=0.6, % y軸の範囲
                    xtick=\empty,        % x軸の目盛りはなし
                    ytick=\empty,        % y軸の目盛りはなし
                    axis equal,          % 縦横比を1:1に
                    clip=false,          % 描画エリア外へのラベル表示を許可
                ]

                % --- 主要な座標を定義 ---
                \coordinate (P0) at (0,0);      % 原点 P_0
                \coordinate (P1) at (1,0);      % 点 P_1
                \coordinate (Xref) at (2,0);   % 角度の基準となるx軸上の点

                % --- ベクトルの角度と長さを設定 ---
                \def\myangle{145}   % 角度θ (度数法)
                \def\veclength{1.0} % ベクトルの長さ

                % --- ベクトルの終点を計算 ---
                \coordinate (V1) at ({1 + \veclength*cos(\myangle)}, {\veclength*sin(\myangle)});
                \coordinate (V2) at ({1 + \veclength*cos(-\myangle)}, {\veclength*sin(-\myangle)});

                % --- ベクトルを描画 ---
                \draw[-stealth, ultra thick] (P1) -- (V1);
                \draw[-stealth, ultra thick] (P1) -- (V2);

                % --- 角度を示す円弧とラベルを描画 ---
                % tikzlibraryの "angles" と "quotes" を利用
                \pic [draw, "$\theta$", angle eccentricity=1.4, angle radius=0.4cm] {angle = V1--P1--P0};
                \pic [draw, "$\theta$", angle eccentricity=1.4, angle radius=0.4cm] {angle = P0--P1--V2};

                % --- 点のラベルを配置 ---
                \node[below left, inner sep=1pt] at (P0) {$P_0$};
                \draw[ultra thick] (P0) -- (P1);
                \node[below right] at (P1) {$P_1(1,0)$};
                \node[above] at (V1) {$P_2$};
                \node[below] at (V2) {$P_2$};
                \draw (P1) to [out=120, in=-15] (V1);
                \node[above right=0.2cm,fill=white] at ($(P1)!0.5!(V1)$) {$r$};
            \end{axis}
        \end{tikzpicture}
        \caption{原点を$P_0$とする$xy$平面に，$P_1(1,0)$を配置する．}
        \label{2004-1:fig:1}
    \end{figure}

    $y$軸を$\Im$軸, $x$軸を$\Re$軸とする複素平面で考え, $P_n, P_{n+1}$ を表す複素数を $\alpha_n$ と表す．
    また，
    \begin{align*}
        e(\theta) = \cos\theta + i\sin\theta
    \end{align*}
    と置く．
    すると実数数列 $a_n (a_n > 0)$ と $\theta_n$ があって
    \begin{align}
        \alpha_n = a_n e(i\theta_n) \label{2004-1:eq:1}
    \end{align}
    と表すことができる．

    条件(a)から
    \begin{align*}
        a_{n+1} = r a_n
    \end{align*}
    であって，初期条件$a_0 = 1$ から, 等比数列の公式から
    \begin{align}
        a_n = r^n \label{2004-1:eq:2}
    \end{align}
    である．

    \begin{figure}[H]
        \centering
        \begin{tikzpicture}[scale=1.2]
            % --- パラメータ定義 ---
            \def\mytheta{35} % 角度θ (度数法)
            \def\baselength{4} % 三角形の底辺の長さ

            % --- 座標の定義 ---
            % P_n と P_{n+2} をx軸上に配置
            % P_{n+1} の座標を計算 (xは底辺の中点, yはtanを使って計算)
            \coordinate (Pn) at ({\baselength/2}, {\baselength/2 * tan(\mytheta)});
            \coordinate (Pntwo) at (0,0);
            \coordinate (Pnone) at (\baselength,0);
            \coordinate (Pnthree) at ($(Pntwo)!0.4!(Pn)$);


            % --- 描画 ---

            % Pn と Pn+2 を通る直線
            \draw ($(Pn)!-0.4!(Pntwo)$) -- ($(Pntwo)!-0.4!(Pn)$);

            % 三角形の辺 Pn--Pn+1 と Pn+1--Pn+2
            \draw (Pn) -- (Pnone) -- (Pntwo);

            % --- ラベル配置 ---
            % 頂点ラベル
            \node[above] at (Pn) {$P_n$};
            \node[below] at (Pnone) {$P_{n+1}$};
            \node[below] at (Pntwo) {$P_{n+2}$};
            \node[above] at (Pnthree) {$P_{n+2}$};
            \fill[] (Pn) circle (1.5pt);
            \fill[] (Pnone) circle (1.5pt);
            \fill[] (Pntwo) circle (1.5pt);
            \fill[] (Pnthree) circle (1.5pt);


            % % 辺の長さのラベル
            % \node[sloped, midway, above] at (Pn--Pnone) {$a_n=r^n$};
            % \node[sloped, midway, above] at (Pnone--Pntwo) {$r^{n+1}$};

            % 角度のラベルと記号
            % tikzlibraryの "angles" と "quotes" を利用
            \pic [draw, angle eccentricity=1.5, angle radius=0.4cm, "$\theta$"] {angle = Pnone--Pntwo--Pn};
            \pic [draw, angle eccentricity=1.5, angle radius=0.4cm, "$\pi-2\theta$"] {angle = Pntwo--Pn--Pnone};
            \pic [draw, angle eccentricity=1.5, angle radius=0.4cm, "$\theta$"] {angle = Pn--Pnone--Pntwo};

            % % 角の等号記号
            % \draw pic[angle radius=0.6cm, pic text=$test$] {angle = Pn--Pntwo--Pnone};
            % \draw pic[angle radius=0.6cm, pic text=$test$] {angle = Pnone--Pn--Pntwo};
            \draw (Pn) to [out=-15, in=125] (Pnone);
            \node[above right,fill=white] at ($(Pn)!0.5!(Pnone)$) {$a_{n}$};
            \draw (Pnone) to [out=200, in=-20] (Pntwo);
            \node[below=0.2cm,fill=white] at ($(Pnone)!0.5!(Pntwo)$) {$a_{n+1}$};


        \end{tikzpicture}
        \caption{三角形$P_n P_{n+1} P_{n+2}$の様子．}
        \label{2004-1:fig:2}
    \end{figure}


    次に(b)及び(c)から, $\triangle P_n P_{n+1} P_{n+2}$ は\cref{2004-1:fig:2}のようになって，
    $\overline{P_{n+1} P_{n+2}}$ を2通りで表して
    \begin{align*}
         & \overline{P_{n+1} P_{n+2}} = 2\overline{P_{n} P_{n+1}}\cos\theta \\
        \therefore
         & r^{n+1} = 2r^n \cos\theta
    \end{align*}
    である．$r \neq 0$ より
    \begin{align*}
        r = 2 \cos\theta
    \end{align*}
    である．

    (2)
    \cref{2004-1:fig:2}から$P_n P_{n+1}$ から $P_{n+1} P_{n+2}$ へは常に一定の角度だけ回転している．
    よって$\theta_n$ は以下のいずれか一方をみたす．
    \begin{align*}
        \begin{dcases}
            \theta_{n+1} = \theta_n + (\pi - \theta) \\
            \theta_{n+1} = \theta_n + (\pi + \theta)
        \end{dcases}  \label{2004-1:eq:3}
    \end{align*}
    また，初期条件は$\theta_0 = 0$ だから
    \begin{align}
        \theta_n & = n(\pi\pm \theta)
    \end{align}
    である．

    (1)の結果および\cref{2004-1:eq:1}から
    \begin{align*}
        \begin{aligned}
            \alpha_n
             & = r^n e(n\pi \pm n\theta)                 \\
             & = r^n \{e(\pi \pm \theta)\}^n             \\
             & = (2\cos\theta)^n \{e(\pi \pm \theta)\}^n
        \end{aligned}
    \end{align*}
    である．

    したがって$P_n$の表す点の複素数 $Z_n$ として
    \begin{align*}
        Z_n
         & = \sum_{k=0}^{n-1} \alpha_k                                                                              \\
         & = \frac{1 - r^n \{e(\pi\pm \theta)\}^n}{1 - r e(\pi\pm \theta)}                                          \\
         & = \frac{1 - (2\cos\theta)^n (\pm\cos\theta+i\sin\theta)^n}{1 \pm 2\cos\theta(\pm\cos\theta+i\sin\theta)}
    \end{align*}
    である．$\cdots$(答)

    \vspace{10pt}

    (3)
    (2)より$Z_n$が収束する必要十分条件は
    \begin{align*}
         & |2\cos\theta| < 1                       \\
        \therefore
         & \frac{\pi}{3} < \theta < \frac{2\pi}{3}
    \end{align*}
    である．$\cdots$(答)

    \vspace{10pt}

    (4)
    (3)の条件のもとで，(2)の$Z_n$の極限値は
    \begin{align*}
        Z_n
         & \xrightarrow{n \to \infty}
        \frac{1}{1+2\cos^2\theta \mp 2i\sin\theta\cos\theta}                                                    \\
         & = \frac{1+2\cos^2\theta \pm 2i\sin\theta\cos\theta}{(1+2\cos^2\theta)^2 + (2\sin\theta\cos\theta)^2}
    \end{align*}
    だから，実部が$\alpha(\theta)$，虚部が$\beta(\theta)$であって
    \begin{align*}
        \alpha(\theta)
         & = \frac{\cos2\theta+2}{(\cos2\theta+2)^2 + \sin^2 2\theta}   \\
         & = \frac{2+\cos2\theta}{5+4\cos2\theta}                       \\
        \beta(\theta)
         & = \frac{\pm \sin2\theta}{(\cos2\theta+2)^2 + \sin^2 2\theta} \\
         & = \frac{\pm \sin2\theta}{5+4\cos2\theta}
    \end{align*}
    と書ける．
    $\theta\to\frac{\pi}{3}+0$での極限は
    \begin{align*}
        \begin{dcases}
            \lim_{\theta\to\frac{\pi}{3}+0} \alpha(\theta) =  \frac{1}{2} \\
            \lim_{\theta\to\frac{\pi}{3}+0} \beta(\theta)  =   \pm\frac{\sqrt{3}}{6}
        \end{dcases}
    \end{align*}
    である．$\cdots$(答)

    \vspace{10pt}

    (5)
    $\frac{\pi}{3} < \theta < \frac{\pi}{2}$の範囲では$\beta(\theta)$の複合正は正の値をとり，複合負は負の値を取る．
    従って，$\beta(\theta)$が最大になるのは複合正の方である．
    そこでこれを$\beta_{+}(\theta)$と置く．
    \begin{align*}
        \beta_{+}(\theta)
         & = \frac{\sin2\theta}{5+4\cos2\theta}                      \\
         & = \frac{1}{4} \frac{\sin2\theta}{\frac{5}{4}+\cos2\theta}
    \end{align*}
    である．
    この後半の分数は，単位円上の点 $(\cos 2\theta, \sin 2\theta)\, \left(\frac{2}{3}\pi < 2\theta < \pi\right)$
    と 点 $\left(-\frac{5}{4}, 0\right)$ との傾きを表すから，図より
    \begin{align*}
        0 < k \le \frac{4}{3}
    \end{align*}
    である．右側の等号成立の条件は図で直線$l$と円が接する時である．

    \begin{figure}[H]
        \centering
        \begin{tikzpicture}
            \begin{axis}[
                    axis lines=middle,   % 軸を原点を通るように描画
                    xlabel=$x$,          % x軸ラベル
                    ylabel=$y$,          % y軸ラベル
                    xmin=-2.5, xmax=1.5, % x軸の範囲
                    ymin=-1.5, ymax=1.5, % y軸の範囲
                    axis equal,          % 円が歪まないように縦横比を1:1に
                    xtick={-2, -1.25},   % x軸の目盛り
                    xticklabels={-2, $-\frac{5}{4}$}, % 目盛りのラベル
                    ytick=\empty,        % y軸の目盛りはなし
                    clip=false,          % 描画エリア外への描画を許可
                ]

                % --- 主要な座標を定義 ---
                \coordinate (P) at (-1.25, 0); % 外部の点 (-5/4, 0)
                \coordinate (T1) at (-0.8, 0.6);  % 上側の接点 (-4/5, 3/5)
                \coordinate (T2) at (-0.8, -0.6); % 下側の接点 (-4/5, -3/5)
                \coordinate (O) at (0, 0);      % 原点
                \coordinate (T4) at (-1, 0);      % 原点
                \coordinate (T3) at (-1/2, {sqrt(3)/2});      % 原点
                \coordinate (X) at (1, 0);      % 原点


                % --- 2本の接線 y = ±(4/3)(x + 5/4) を描画 ---
                % 線の両端に矢印を描画
                \addplot[domain=-2.2:-0.5, samples=2, thick, red] {4/3 * (x + 5/4)};
                \addplot[domain=-2.2:-0.3, samples=2, dashed] {1/sqrt(3) * (x + 2)};

                % --- 単位円 x^2 + y^2 = 1 を描画 ---
                \addplot[domain=0:360, samples=101, smooth] ({cos(x)}, {sin(x)});
                \addplot[domain=120:180, samples=101, smooth, ultra thick] ({cos(x)}, {sin(x)});


                % --- 原点から接点への半径を破線で描画 ---
                \draw[dashed] (O) -- (T1);
                \draw[dashed] (O) -- (T2);
                \draw[dashed] (O) -- (T3);

                % --- 点を黒丸でマーク ---
                \fill (P) circle (1.5pt);
                \fill (T1) circle (1.5pt);
                \fill (T2) circle (1.5pt);
                \fill (T3) circle (1.5pt);
                \fill (T4) circle (1.5pt);

                % --- 接点に直角記号を描画 ---
                \pic [draw, angle eccentricity=1.5, angle radius=0.2cm] {right angle = P--T1--O};
                \pic [draw, angle eccentricity=2.0, "$\frac{2\pi}{3}$",angle radius=0.2cm] {angle = X--O--T3};
                % \pic [draw, angle eccentricity=1.5, angle radius=0.2cm] {right angle = P--T2--O};
            \end{axis}
        \end{tikzpicture}
        \caption{単位円上の点 $(\cos 2\theta, \sin 2\theta)\, \left(\frac{2}{3}\pi < 2\theta < \pi\right)$と点 $\left(-\frac{5}{4}, 0\right)$ との傾き}
        \label{2004-1:fig:4}
    \end{figure}

    \begin{figure}[H]
        \centering
        \begin{tikzpicture}
            \begin{axis}[
                    axis lines=middle,   % 軸を原点を通るように描画
                    xlabel=$x$,          % x軸ラベル
                    ylabel=$y$,          % y軸ラベル
                    xmin=-1.5, xmax=0, % x軸の範囲
                    ymin=0, ymax=1.5, % y軸の範囲
                    axis equal,          % 円が歪まないように縦横比を1:1に
                    xtick={-1.25},   % x軸の目盛り
                    xticklabels={$-\frac{5}{4}$}, % 目盛りのラベル
                    ytick=\empty,        % y軸の目盛りはなし
                    clip=false,          % 描画エリア外への描画を許可
                ]

                % --- 主要な座標を定義 ---
                \coordinate (P) at (-1.25, 0); % 外部の点 (-5/4, 0)
                \coordinate (T1) at (-0.8, 0.6);  % 上側の接点 (-4/5, 3/5)
                \coordinate (T2) at (-0.8, -0.6); % 下側の接点 (-4/5, -3/5)
                \coordinate (O) at (0, 0);      % 原点
                \coordinate (T4) at (-1, 0);      % 原点
                \coordinate (T3) at (-1/2, {sqrt(3)/2});      % 原点
                \coordinate (X) at (1, 0);      % 原点

                % --- 2本の接線 y = ±(4/3)(x + 5/4) を描画 ---
                % 線の両端に矢印を描画
                \addplot[domain=-1.5:-0.5, samples=2, thick, red] {4/3 * (x + 5/4)};

                % --- 単位円 x^2 + y^2 = 1 を描画 ---
                \addplot[domain=90:180, samples=101, smooth] ({cos(x)}, {sin(x)});
                \addplot[domain=120:180, samples=101, smooth, ultra thick] ({cos(x)}, {sin(x)});

                % --- 原点から接点への半径を破線で描画 ---
                \draw[dashed] (O) -- (T1);

                % --- 点を黒丸でマーク ---
                \fill (P) circle (1.5pt);
                \fill (T1) circle (1.5pt);
                \fill (T4) circle (1.5pt);

                % --- 接点に直角記号を描画 ---
                \pic [draw, angle eccentricity=1.5, angle radius=0.2cm] {right angle = P--T1--O};
                \node[above right] at (-1/2,0.4) {$1$};
                \node[above] at (-1.1,0.4) {$4/3$};
                \pic [draw, angle eccentricity=1.5, angle radius=0.2cm] {angle = O--P--T1};
            \end{axis}
        \end{tikzpicture}
        \caption{単位円上の点 $(\cos 2\theta, \sin 2\theta)\, \left(\frac{2}{3}\pi < 2\theta < \pi\right)$と点 $\left(-\frac{5}{4}, 0\right)$ との傾きが最大となる時の様子．}
        \label{2004-1:fig:5}
    \end{figure}

    従って，
    \begin{align*}
        0 < \beta_{+}(\theta) \le \frac{1}{3}
    \end{align*}
    である．よって最大値は
    \begin{align*}
        \max \beta(\theta) = \frac{1}{3}
    \end{align*}
    である．


    {\bf[解説]}

    \newpage
\end{multicols}
\end{document}