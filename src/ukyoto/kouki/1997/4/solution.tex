\documentclass[../../../../main.tex]{subfiles}
% \documentclass[a4paper,10pt]{ltjsarticle}
\begin{document}
\rhead{京都大学後期$1997$年$4$番}
\setcounter{equation}{0}
\begin{oframed}
  \input{problem.tex}
\end{oframed}
\setlength{\columnseprule}{0.4pt}
\begin{multicols}{2}
  {\bf[解]}
  (1)
  $(a,b,c)$が与えられた連立方程式の解だから
  \begin{align}
    \begin{dcases}
      b = 2a^2 - 1 \\
      c = 2b^2 - 1 \\
      a = 2c^2 - 1
    \end{dcases} \label{1997-4:eq:1}
  \end{align}
  が満たされる．

  背理法により題意を示す．
  $|a|>1$と仮定すると，\cref{1997-4:eq:1}より$b>1,c>1,a>1$となる．
  従ってこの時，$a^2>a$などの不等式が成り立つ．
  \cref{1997-4:eq:1}の全ての式を足し上げると
  \begin{align*}
    a+b+c = 2(a^2+b^2+c^2)-3 > 2\left(a+b+c\right)-3 \\
    \therefore
    a+b+c <3
  \end{align*}
  を得るが，これは$a,b,c>1$に反して矛盾．よって背理法により$|a|\le 1$であり，
  この時\cref{1997-4:eq:1}から$|b|\le 1, |c|\le 1$が従う．
  よって題意は示された．
  $\cdots$(答)

  \vspace{10pt}
  (2)
  (1)から
  \begin{align*}
     & a = \cos \alpha & (0 \leqq \alpha < 2\pi)
  \end{align*}
  とおける．\cref{1997-4:eq:1}および半角公式から
  \begin{align*}
    b & = \cos 2\alpha                \\
    c & = \cos 4\alpha                \\
    a & =  \cos \alpha = \cos 8\alpha
  \end{align*}
  なる関係を得る．従って，最後の式を解く．和積公式から
  \begin{align*}
     & \cos 8\alpha - \cos \alpha = 0                      \\
     & -2\sin \frac{9\alpha}{2} \sin \frac{7\alpha}{2} = 0
  \end{align*}
  であるから
  \begin{align*}
    \sin \frac{9\alpha}{2} = 0 \lor \sin \frac{7\alpha}{2}
  \end{align*}
  である．$0 \leqq \alpha < 2\pi$ より$\alpha$は
  \begin{align*}
    \alpha =
    \begin{dcases}
      \frac{4}{9} n \pi & (n=0,1,2,3,4) \\
      \frac{4}{7} k \pi & (k=0,1,2,3)
    \end{dcases}
  \end{align*}
  と書ける．$n=k=0$の時は$\alpha=0$で等しくなるが，そのほかの$n,k$では$\alpha$は相異なる値をとる．
  従って，求める解の個数は$n=0,1,2,3,4$, $k=1,2,3$の合計$8$個である．$\cdots$(答)

  \vspace{10pt}
  {\bf[解説]}

  面白い問題で，色々と解法が考えられるところだ．
  まず(1)からいくつか別解を紹介しよう．いずれも$a,b,c$の対称性を用いることになる．
  まず，$|a|>1$を仮定すると\cref{1997-4:eq:1}から$a,b,c>1$が従うことに気を付ける．
  これと背理法を組み合わせるのが全ての別解に共通するロジックである．

  \textbf{[（1）別解-1]}
  $b-a$を評価すると
  \begin{align*}
    b-a
     & = 2a^2-a-1      \\
     & = (a-1)(2a+1)>0 \\
    \therefore
     & b>a
  \end{align*}
  だが，同様の評価を繰り返すと $a>c>b>a$ となり矛盾．

  \vspace{10pt}
  \textbf{[（1）別解-2]}
  $a-1$を評価すると
  \begin{align*}
    a-1
     & = 2c^2-2                   \\
     & = 2(c+1)(c-1)              \\
     & = 2(c+1) \cdot 2(b+1)(b-1) \\
     & = 8(c+1)(b+1)(a+1)(a-1)
  \end{align*}
  となり，$a-1 \ne 0$ で両辺を割れば
  \begin{align*}
    1 = 8(c+1)(b+1)(a+1)
  \end{align*}
  となり矛盾する．


  (2)の方も別解が考えられるので紹介しよう．
  対称性を使わないのであまり綺麗な方法ではないのだが，
  与えられた方程式は$3$変数なので，$1$変数消してしまおうという方針である．
  \cref{1997-4:eq:1}からbを消去すると
  \begin{align*}
    c = 2(2a^2-1)^2-1
  \end{align*}
  この右辺を$f(a)$とすると$f(a)$は$a$の4次関数で、$ac$平面上で
  $(0,1)$, $(\pm 1,1)$, $(\pm \frac{1}{\sqrt{2}}, -1)$を通り，
  \begin{align*}
    a = 2c^2-1
  \end{align*}
  と合わせてグラフは\cref{1997-4:fig:1}のようになる．

  \begin{figure}[H]
    \begin{tikzpicture}
      \begin{axis}[
          axis lines=middle,
          xlabel=$a$,
          ylabel=$c$,
          xmin=-1.5, xmax=1.5,
          ymin=-1.5, ymax=1.5,
          xtick={-1, -0.7071, 0, 0.7071, 1}, % Approximately -1/sqrt(2) and 1/sqrt(2)
          xticklabels={-1, $-\frac{1}{\sqrt{2}}$, 0, $\frac{1}{\sqrt{2}}$, 1},
          ytick={-1, 0, 1},
          grid=none,
          clip=false, % Allow drawing outside the axis limits if needed for connecting lines
          declare function={
              f(\x) = 2*(2*\x^2-1)^2-1;
              g(\x) = sqrt((\x+1)/2); % For a = 2c^2-1, c = sqrt((a+1)/2)
            }
        ]

        % Plot c = 2(2a^2-1)^2-1
        \addplot[domain=-1.05:1.05, samples=200, smooth, thick, blue] {f(x)};

        % Plot a = 2c^2-1, which means c = \pm\sqrt{(a+1)/2}
        % Positive branch
        \addplot[domain=-1:1.1, samples=200, smooth, thick, red] {g(x)};
        % Negative branch
        \addplot[domain=-1:1.1, samples=200, smooth, thick, red] {-g(x)};

        % Mark key points on c = 2(2a^2-1)^2-1
        \addplot[only marks, mark=*, mark size=2pt] coordinates {
            (1,1)
            (-1,1)
            (0.7071,-1) % (1/sqrt(2), -1)
            (-0.7071,-1) % (-1/sqrt(2), -1)
          };

        % Mark intersection points
        \addplot[only marks, mark=*, mark size=2pt, fill=black] coordinates {
            (1,1)
            (-1,1) % from the first graph, also close to the intersection in the image
            % (-0.825, 0.565) % approximate intersection
            % (-0.966, -0.259) % approximate intersection
            % (-0.259, -0.966) % approximate intersection
            % (0.565, -0.825) % approximate intersection
            % (0.966, -0.259) % approximate intersection
            % (0.825, 0.565) % approximate intersection
          };

        % Add bounding box lines for -1 to 1 for both a and c
        \draw[dashed] (axis cs:-1,-1) rectangle (axis cs:1,1);
        \draw[dashed]  (axis cs:0.7071,-1) --  (axis cs:0.7071,1) ;
        \draw[dashed]  (axis cs:-0.7071,-1) --  (axis cs:-0.7071,1) ;

      \end{axis}
    \end{tikzpicture}
    \caption{$ac$平面に描いた連立方程式の様子}
    \label{1997-4:fig:1}
  \end{figure}

  与えられた連立方程式の解の個数は，まさにグラフ上の二本の曲線の交点の数に他ならないから，
  求める解の個数は$8$個である．
  $\cdots$(答)



  \newpage
\end{multicols}
\end{document}