\documentclass[../../../../main.tex]{subfiles}
% \documentclass[a4paper,10pt]{ltjsarticle}
\begin{document}
\rhead{京都大学後期$2001$年$3$番}
\setcounter{equation}{0}
\begin{oframed}
  \input{../../../problems/2001/3/problem.tex}
\end{oframed}
\setlength{\columnseprule}{0.4pt}
\begin{multicols}{2}
  {\bf[解]}
  （1）
  複素数$z$の表す点を$Z$と大文字で書いて区別する．
  座標原点を$O$とする．

  また，表記の簡潔さのため
  \begin{align}
    e(x)   & = \cos x + i\sin x                   \\
    \theta & = \frac{2\pi}{5} \label{2001-3:eq:1}
  \end{align}
  とおく．$e(x)$は
  \begin{align*}
    e(x+y) = e(x)e(y)
  \end{align*}
  が成り立つことに注意する．
  題意の$\alpha$は
  \begin{align*}
    \alpha = e\left(\theta\right)
  \end{align*}
  である．
  題意の正五角形の頂点の座標は$\alpha^{k}\, (k=0,1,2,3,4)$である．
  これらの点を$A_k$とおく．
  すると，題意の点$Z$は直線 $A_1A_2$ と $A_0A_4$ の交点であり，
  概形は\cref{2001-3:fig:1}のようになる．


  \begin{figure}[H]
    \centering
    \begin{tikzpicture}
      \begin{axis}[
          xmin=-1.5, xmax=2.5,
          ymin=-1.5, ymax=2,
          axis lines=middle,
          axis equal,
          xlabel=$x$,
          ylabel=$y$,
          xtick=\empty, ytick=\empty, % 目盛りを非表示
          tick style={draw=none}, % 目盛りの線を非表示
          clip=false
        ]

        % 単位円
        \addplot[thick, dashed, gray!70] coordinates {(0,0)} circle (1);

        % 正五角形の頂点
        \pgfmathsetmacro{\alphaX}{cos(deg(2*pi/5))}
        \pgfmathsetmacro{\alphaY}{sin(deg(2*pi/5))}
        \pgfmathsetmacro{\alphaTwoX}{cos(deg(4*pi/5))}
        \pgfmathsetmacro{\alphaTwoY}{sin(deg(4*pi/5))}
        \pgfmathsetmacro{\alphaThreeX}{cos(deg(6*pi/5))}
        \pgfmathsetmacro{\alphaThreeY}{sin(deg(6*pi/5))}
        \pgfmathsetmacro{\alphaFourX}{cos(deg(8*pi/5))}
        \pgfmathsetmacro{\alphaFourY}{sin(deg(8*pi/5))}

        % 頂点の座標を定義 (P_k = \alpha^k)
        \coordinate (P0) at (1,0);
        \coordinate (P1) at (\alphaX, \alphaY);
        \coordinate (P2) at (\alphaTwoX, \alphaTwoY);
        \coordinate (P3) at (\alphaThreeX, \alphaThreeY);
        \coordinate (P4) at (\alphaFourX, \alphaFourY);

        % 正五角形の辺
        \draw[thick] (P0) -- (P1) -- (P2) -- (P3) -- (P4) -- (P0);

        % 頂点のラベル
        \node[above right] at (P0) {}; % A0 はラベルなし
        \node[above left] at (P1) {$A_1(\alpha)$};
        \node[left] at (P2) {$\alpha^2$};
        \node[below left] at (P3) {$\alpha^3$};
        \node[below right] at (P4) {$\alpha^4$};

        % 点Zのプロット 
        \pgfmathsetmacro{\Zreal}{-cos(deg(6*pi/5)) + cos(deg(2*pi/5)) + 1}
        \pgfmathsetmacro{\Zimag}{-sin(deg(6*pi/5)) + sin(deg(2*pi/5))}
        \coordinate (Z) at (\Zreal, \Zimag);
        \node[above right] at (Z) {$Z$};
        \fill (Z) circle (1.5pt); % Zの点を描画

        % 点線と長さのラベル
        \draw[dashed] (P0) -- (Z);
        %\node[above right, xshift=-2mm, yshift=-1mm] at ( $(P0)!0.5!(Z)$ ) {$1+x$};

        \draw[dashed] (P1) -- (Z);
        %\node[below right, xshift=2mm, yshift=1mm] at ( $(P4)!0.5!(Z)$ ) {$1+x$};

        % % 1+x = 1/x のテキスト
        % \node[right, align=left, font=\normalsize] at (axis cs:2.3, 0.5) {$1+x$\\$\|$ \\ $\frac{1}{x}$};

        % % その他の線と直角記号 (手書き図の雰囲気に合わせて)
        \draw[dashed] (P1) -- (0,0); % 原点からA1への線
        \draw[dashed] (P0) -- (P2); % A0からA2への対角線 (長さは1+x)
        \draw[dashed] (P1) -- (P3); % A1からA3への対角線
        \draw[dashed] (P2) -- (P4); % A2からA4への対角線

        % % A0-A2の線上に1+xのラベルを配置
        % \node[below right, xshift=-1mm, yshift=0mm] at ( $(P0)!0.4!(P2)$ ) {$1+x$};

        % % A1-A2の線上に1のラベルを配置
        % \node[above left, xshift=0mm, yshift=-1mm] at ( $(P1)!0.5!(P2)$ ) {$1$};

        % % A0-A4の線上に1のラベルを配置
        % \node[below right, xshift=0mm, yshift=1mm] at ( $(P0)!0.5!(P4)$ ) {$1$};

        % % A1-A0の線上に1のラベルを配置
        % \node[above right, xshift=0mm, yshift=0mm] at ( $(P0)!0.5!(P1)$ ) {$1$};

        % % A1からA3の対角線の中点と、そこから垂線のようなもの
        % % これは描くのが難しいので、手書きの「H」と直角記号の雰囲気を再現
        % % 図の右上の「A(d)」と「1」の近くの直角
        % \path (P1) -- (P2) node[midway] (M12) {};
        % \path (0,0) -- (M12) node[midway] (M0M12) {};
        % % 直角記号 (厳密な位置ではないが、雰囲気を再現)
        % \draw[densely dotted] (\alphaX, \alphaY) -- ($(P0)!0.5!(P2)$); % A1からA0A2の中点へ？
        % \draw[densely dotted] (P0) -- (P1) node[midway] (M01) {};
        % \draw[densely dotted] (P2) -- (P4) node[midway] (M24) {}; % A2からA4への線

        % % 複雑な直角記号の再現 (正確な位置ではないが、図の雰囲気を再現)
        % \coordinate (perp1a) at ($(P0)!0.3!(P2)$);
        % \coordinate (perp1b) at ($(P0)!0.3!(P2)!-0.3cm!90:(P2)$);
        % \draw (perp1a) -- ($(perp1a)+(0.15,0)$) -- ($(perp1a)+(0.15,-0.15)$); % 垂直記号の例

        % % もう一つの直角記号
        % \coordinate (perp2a) at ($(P1)!0.5!(P2)$);
        % \coordinate (perp2b) at ($(perp2a)!0.2cm!90:(P2)$);
        % %\draw[densely dotted] (perp2b) -- (perp2a); % 雰囲気

        % % A(d)と書かれている点
        % \fill (P1) circle (1.5pt); % A1の点を強調
        % \node[above] at (P1) {$A_1(\alpha)$}; % A1(\alpha)のラベル

        % % 右側の斜めの線と、そこからZへの線
        % % (P1)から(Z)への線は図では描かれていないように見えるので省略

      \end{axis}
    \end{tikzpicture}
    \caption{正五角形と$Z$の関係}
    \label{2001-3:fig:1}
  \end{figure}

  \begin{figure}[H]
    \centering
    \begin{tikzpicture}
      \begin{axis}[
          xmin=-1.5, xmax=2.5,
          ymin=-1.5, ymax=2,
          axis lines=middle,
          axis equal,
          xlabel=$x$,
          ylabel=$y$,
          xtick=\empty, ytick=\empty, % 目盛りを非表示
          tick style={draw=none}, % 目盛りの線を非表示
          clip=false
        ]

        % 単位円
        \addplot[thick, dashed, gray!70] coordinates {(0,0)} circle (1);

        % 正五角形の頂点
        \pgfmathsetmacro{\alphaX}{cos(deg(2*pi/5))}
        \pgfmathsetmacro{\alphaY}{sin(deg(2*pi/5))}
        \pgfmathsetmacro{\alphaTwoX}{cos(deg(4*pi/5))}
        \pgfmathsetmacro{\alphaTwoY}{sin(deg(4*pi/5))}
        \pgfmathsetmacro{\alphaThreeX}{cos(deg(6*pi/5))}
        \pgfmathsetmacro{\alphaThreeY}{sin(deg(6*pi/5))}
        \pgfmathsetmacro{\alphaFourX}{cos(deg(8*pi/5))}
        \pgfmathsetmacro{\alphaFourY}{sin(deg(8*pi/5))}

        % 頂点の座標を定義 (P_k = \alpha^k)
        \coordinate (P0) at (1,0);
        \coordinate (P1) at (\alphaX, \alphaY);
        \coordinate (P2) at (\alphaTwoX, \alphaTwoY);
        \coordinate (P3) at (\alphaThreeX, \alphaThreeY);
        \coordinate (P4) at (\alphaFourX, \alphaFourY);

        % 正五角形の辺
        \draw[dashed] (P0) -- (P1) -- (P2) -- (P3) -- (P4) -- (P0);
        \draw[thick] (P0) -- (P1) -- (P2);

        % 頂点のラベル
        \node[above right] at (P0) {}; % A0 はラベルなし
        \node[above left] at (P1) {$A_1(\alpha)$};
        \node[left] at (P2) {$\alpha^2$};
        \node[below left] at (P3) {$\alpha^3$};
        \node[below right] at (P4) {$\alpha^4$};

        % 点Zのプロット 
        \pgfmathsetmacro{\Zreal}{-cos(deg(6*pi/5)) + cos(deg(2*pi/5)) + 1}
        \pgfmathsetmacro{\Zimag}{-sin(deg(6*pi/5)) + sin(deg(2*pi/5))}
        \coordinate (Z) at (\Zreal, \Zimag);
        \node[above right] at (Z) {$Z$};
        \fill (Z) circle (1.5pt); % Zの点を描画

        % 点線と長さのラベル
        \draw[thick,red] (P0) -- (Z);
        \draw[thick,red] (P1) -- (Z);
        \draw[thick,red] (P0) -- (P2);

        % % 1+x = 1/x のテキスト
        % \node[right, align=left, font=\normalsize] at (axis cs:2.3, 0.5) {$1+x$\\$\|$ \\ $\frac{1}{x}$};

        % % その他の線と直角記号 (手書き図の雰囲気に合わせて)
        \draw[dashed] (P1) -- (0,0); % 原点からA1への線

        % A0-A2の線上に1+xのラベルを配置
        % \node[below right, xshift=-1mm, yshift=0mm] at ( $(P0)!0.4!(P2)$ ) {$1+x$};

        % A1-A2/A1-A0の線上にoのラベルを配置
        \node[] at ( $(P1)!0.5!(P2)$ ) {$\circ$};
        \node[] at ( $(P0)!0.5!(P1)$ ) {$\circ$};
        % A0-Z/A0-A2の線上に△のラベルを配置
        \node[] at ( $(P0)!0.5!(Z)$ ) {$\triangle$};
        \node[] at ( $(P2)!0.5!(P0)$ ) {$\triangle$};

        % 角度表示
        \pic[draw, angle radius=7mm, "$\theta/2$", angle eccentricity=1.5,font=\footnotesize] {angle = P0--P2--P1};
        \pic[draw, angle radius=7mm, "$\theta/2$", angle eccentricity=1.5,font=\footnotesize] {angle = P2--Z--P0};
        \pic[draw, angle radius=7mm, "$\pi-\theta$", angle eccentricity=1.5,font=\footnotesize] {angle = Z--P0--P2};
        % % A0-A4の線上に1のラベルを配置
        % \node[below right, xshift=0mm, yshift=1mm] at ( $(P0)!0.5!(P4)$ ) {$1$};

      \end{axis}
    \end{tikzpicture}
    \caption{三角形$ZA_0A_2$と$A_0A_1A_2$の相似関係}
    \label{2001-3:fig:2}
  \end{figure}


  ここで，三角形$A_0A_1Z$は$A_0A_1A_2$と相似で，後者が$|A_1A_0|=|A_1A_2|$の二等辺三角形だから前者も二等辺三角形であり，
  \begin{align*}
    |A_0A_2|         & = |A_0Z|                      \\
    \angle A_0A_1A_2 & = \angle ZA_0A_2 = \pi-\theta
  \end{align*}
  であり，よって$Z$は$A_2$を$A_0$を中心として$-\pi + \theta$回転したもので
  \begin{align}
    z-1
      & = e\left(-\pi + \theta\right)(\alpha^2-1) \\
      & = e(-\pi)e\left(\theta\right)(\alpha^2-1) \\
      & = -\alpha(\alpha^2-1)                     \\
    \therefore
    z & = -\alpha^3+\alpha+1  \label{2001-3:eq:2}
  \end{align}
  である．  $\cdots$(答)

  \vspace{10pt}
  (2)
  題意の円$C$は，要するに三角形$ZA_0A_2$の外接円である．
  三角形$ZA_0A_2$が$|ZA_0|=|A_0A_2|$の二等辺三角形であることから
  $C$の中心$B$は線分 $ZA_2$ の 2 等分線上にある．

  \begin{figure}[H]
    \centering
    \begin{tikzpicture}
      \begin{axis}[
          xmin=-1.5, xmax=2.5,
          ymin=-0.5, ymax=2,
          axis lines=middle,
          axis equal,
          xlabel=$x$,
          ylabel=$y$,
          xtick=\empty, ytick=\empty, % 目盛りを非表示
          tick style={draw=none}, % 目盛りの線を非表示
          clip=false
        ]

        % 単位円
        %\addplot[thick, dashed, gray!70] coordinates {(0,0)} circle (1);

        % 正五角形の頂点
        \pgfmathsetmacro{\alphaX}{cos(deg(2*pi/5))}
        \pgfmathsetmacro{\alphaY}{sin(deg(2*pi/5))}
        \pgfmathsetmacro{\alphaTwoX}{cos(deg(4*pi/5))}
        \pgfmathsetmacro{\alphaTwoY}{sin(deg(4*pi/5))}
        \pgfmathsetmacro{\alphaThreeX}{cos(deg(6*pi/5))}
        \pgfmathsetmacro{\alphaThreeY}{sin(deg(6*pi/5))}
        \pgfmathsetmacro{\alphaFourX}{cos(deg(8*pi/5))}
        \pgfmathsetmacro{\alphaFourY}{sin(deg(8*pi/5))}

        % 頂点の座標を定義 (P_k = \alpha^k)
        \coordinate (O) at (0,0);
        \coordinate (P0) at (1,0);
        \coordinate (P1) at (\alphaX, \alphaY);
        \coordinate (P2) at (\alphaTwoX, \alphaTwoY);
        %\coordinate (P3) at (\alphaThreeX, \alphaThreeY);
        %\coordinate (P4) at (\alphaFourX, \alphaFourY);

        % 正五角形の辺
        \draw[dashed] (P0) -- (P1) -- (P2) -- cycle;
        \draw[thick] (P0) -- (P1) -- (P2) -- cycle;
        % 頂点のラベル
        \node[below right] at (P0) {$1$};
        \node[above left] at (P1) {$\alpha$};
        \node[left] at (P2) {$\alpha^2$};

        % 点Zのプロット 
        \pgfmathsetmacro{\Zreal}{-cos(deg(6*pi/5)) + cos(deg(2*pi/5)) + 1}
        \pgfmathsetmacro{\Zimag}{-sin(deg(6*pi/5)) + sin(deg(2*pi/5))}
        \coordinate (Z) at (\Zreal, \Zimag);
        \node[above right] at (Z) {$Z$};
        \fill (Z) circle (1.5pt); % Zの点を描画

        % 点線と長さのラベル
        \draw[thick] (P0) -- (Z);
        \draw[thick] (P1) -- (Z);
        \draw[thick] (P0) -- (P2);
        \draw[dashed] (P1) -- (0,0); % 原点からA1への線

        % 点Bのプロット 
        \pgfmathsetmacro{\Breal}{1/2}
        \pgfmathsetmacro{\Bimag}{tan(deg(2*pi/5))/2}
        \coordinate (B) at (\Breal, \Bimag);
        \node[above right] at (B) {$B$};
        \fill (B) circle (1.5pt); % Zの点を描画

        % 点線と長さのラベル
        \draw[thick,red] (P0) -- (B) -- (0,0);
        \draw[thick,red] (Z) -- (B) -- (P2);
        \draw[thick] (P1) -- (Z);
        \draw[thick] (P0) -- (P2);
        \draw[dashed] (O) -- (P2);


        \node[] at ( $(B)!0.5!(Z)$ ) {$\square$};
        \node[] at ( $(B)!0.5!(P0)$ ) {$\square$};
        \node[] at ( $(B)!0.5!(0,0)$ ) {$\square$};
        \node[] at ( $(B)!0.5!(P2)$ ) {$\square$};

        % A1-A2/A1-A0の線上にoのラベルを配置
        \node[] at ( $(P1)!0.5!(P2)$ ) {$\circ$};
        \node[] at ( $(P0)!0.5!(P1)$ ) {$\circ$};

        % 角度表示
        \pic[draw, angle radius=3mm, "$\theta$", angle eccentricity=1.5,font=\small] {angle = P0--O--P1};
        \pic[draw, angle radius=3mm, "$\theta$", angle eccentricity=1.5,font=\small] {angle = B--P0--O};

      \end{axis}
    \end{tikzpicture}
    \caption{円$C$の中心$B$と，$A_0,A_2,Z$の関係．}
    \label{2001-3:fig:3}
  \end{figure}

  次に，三角形$OA_0A_2$が二等辺三角形であることと，
  $B$が$C$の中心ゆえ$|BA_2|=|BA_0|$であることから，
  線分$OB$は$A_0A_2$と直交する二等分線である．これらの交点を$H$とおく．

  題意の条件，点$O$も$C$上にあることを示すには
  \begin{align*}
    |BO| = |BA_0|
  \end{align*}
  を示せば良い．
  すなわち，三角形$OA_0B$が$|BO| = |BA_0|$の二等辺三角形であることを示せば良い．

  まず，$\angle BOA_0$について，線分$OA_1$も線分$A_0A_1$の二等分線だから
  直線$OA_1$と直線$OB$は等しく
  \begin{align*}
    \angle BOA_0
     & = \angle BOA_1 \\
     & = \theta
  \end{align*}
  である．

  次に$\angle BA_0O$について，
  \begin{align*}
    \angle BA_0O
     & = \angle OA_0A_2 + \angle A_2A_0B              \\
     & = \angle OA_0A_2 + \frac{\angle A_2A_0Z}{2}    \\
     & = \frac{\pi-2\theta}{2} + \frac{\pi-\theta}{2} \\
     & = \pi-\frac{3\theta}{2}                        \\
     & = \frac{2\pi}{5}                               \\
     & = \theta
  \end{align*}
  である．

  以上から$\angle BOA_0=\angle BA_0O$であり，従って
  \begin{align*}
    |BO| = |BA_0|
  \end{align*}
  となり，$O$も確かに円$C_1$上にあるから題意は示された．$\cdots$(答)


  \vspace{10pt}
  {\bf[解説]}

  (1)は導出によって，いくつか見た目の異なる表現が得られる．
  その一つが
  \begin{align*}
    z = \frac{\alpha(\alpha+1)}{\alpha^2+1}
  \end{align*}
  である．これは$Z$ を $A_0$ のまわりに $\frac{2}{5}\pi$ だけ回転し，$A_0$ からの距離を $\frac{A_1A_0}{A_0Z}$ 倍したものが $A_1$ である．
  として立式したときにこの形になる．
  解答の表現と同一の値を与えることは以下のように確認できる．
  \begin{align*}
    (-\alpha^3+\alpha+1)(\alpha^2+1)
     & = -\alpha^5+\alpha^3+\alpha^2-\alpha^3+\alpha+1 \\
     & = -1 +\alpha^2+\alpha+1                         \\
     & = \alpha(\alpha+1)
  \end{align*}
  より，この両辺を$\alpha^2+1$で割れば良い．
  ただし，二行目で$\alpha^5=1$を利用した．

  (2)はより複素数チックに解く方法として，
  円周角の定理を用いて$\angle A_2OZ=\angle A_2A_0Z$を示す方針でも良い．
  以下これを示そう．
  そこで
  \begin{align*}
     & \angle A_2OZ  = \theta_1  \\
     & \angle A_2A_0Z = \theta_2
  \end{align*}
  とおく．

  \begin{figure}[H]
    \centering
    \begin{tikzpicture}
      \begin{axis}[
          xmin=-1.5, xmax=2.5,
          ymin=-0.5, ymax=2,
          axis lines=middle,
          axis equal,
          xlabel=$x$,
          ylabel=$y$,
          xtick=\empty, ytick=\empty, % 目盛りを非表示
          tick style={draw=none}, % 目盛りの線を非表示
          clip=false
        ]

        % 単位円
        %\addplot[thick, dashed, gray!70] coordinates {(0,0)} circle (1);

        % 正五角形の頂点
        \pgfmathsetmacro{\alphaX}{cos(deg(2*pi/5))}
        \pgfmathsetmacro{\alphaY}{sin(deg(2*pi/5))}
        \pgfmathsetmacro{\alphaTwoX}{cos(deg(4*pi/5))}
        \pgfmathsetmacro{\alphaTwoY}{sin(deg(4*pi/5))}
        \pgfmathsetmacro{\alphaThreeX}{cos(deg(6*pi/5))}
        \pgfmathsetmacro{\alphaThreeY}{sin(deg(6*pi/5))}
        \pgfmathsetmacro{\alphaFourX}{cos(deg(8*pi/5))}
        \pgfmathsetmacro{\alphaFourY}{sin(deg(8*pi/5))}

        % 頂点の座標を定義 (P_k = \alpha^k)
        \coordinate (O) at (0,0);
        \coordinate (P0) at (1,0);
        \coordinate (P1) at (\alphaX, \alphaY);
        \coordinate (P2) at (\alphaTwoX, \alphaTwoY);
        %\coordinate (P3) at (\alphaThreeX, \alphaThreeY);
        %\coordinate (P4) at (\alphaFourX, \alphaFourY);

        % 正五角形の辺
        \draw[dashed] (P0) -- (P1) -- (P2) -- cycle;
        % 頂点のラベル
        \node[below right] at (P0) {$1$};
        \node[above left] at (P1) {$\alpha$};
        \node[left] at (P2) {$\alpha^2$};

        % 点Zのプロット 
        \pgfmathsetmacro{\Zreal}{-cos(deg(6*pi/5)) + cos(deg(2*pi/5)) + 1}
        \pgfmathsetmacro{\Zimag}{-sin(deg(6*pi/5)) + sin(deg(2*pi/5))}
        \coordinate (Z) at (\Zreal, \Zimag);
        \node[above right] at (Z) {$Z$};
        \fill (Z) circle (1.5pt); % Zの点を描画

        % 点線と長さのラベル
        \draw[thick] (P2) -- (P0) -- (Z);
        \draw[thick] (P2) -- (O) -- (Z);
        \draw[dashed] (P1) -- (Z);
        \draw[dashed] (P1) -- (0,0); % 原点からA1への線

        % 角度表示
        \pic[draw, angle radius=3mm, "$\theta_1$", angle eccentricity=1.5,font=\small] {angle = Z--O--P2};
        \pic[draw, angle radius=3mm, "$\theta_2$", angle eccentricity=1.5,font=\small] {angle = Z--P0--P2};

      \end{axis}
    \end{tikzpicture}
    \caption{$\angle ZA_0A_2$と$\angle ZOA_2$の様子．}
    \label{2001-3:fig:4}
  \end{figure}

  まず$\theta_1$は
  \begin{align*}
    \theta_1
     & = \arg\frac{\alpha^2}{z} \\
     & = 2\arg \alpha - \arg z
  \end{align*}
  である．ここで$A_3,O,Z$がこの順で同一直線上にあることから
  \begin{align*}
    \arg z
     & = \arg \alpha^3 - \pi \\
     & = 3\arg \alpha - \pi
  \end{align*}
  であるから，
  \begin{align*}
    \theta_1
     & = 2 \arg \alpha - (3\arg \alpha - \pi) \\
     & = \pi - \arg \alpha
  \end{align*}
  ここで\cref{2001-3:eq:1}より$\arg\alpha=2\pi/5$だから，
  \begin{align*}
    \theta_1 = \frac{3\pi}{5}
  \end{align*}
  である．

  次に$\theta_2$は
  \begin{align*}
    \theta_2
     & =\arg\frac{\alpha^2-1}{z-1}                 \\
     & =\arg\frac{\alpha^2-1}{-\alpha^3+\alpha}    \\
     & =\arg\frac{\alpha^2-1}{\alpha(-\alpha^2+1)} \\
     & =\arg\frac{\alpha^2-1}{\alpha(-\alpha^2+1)} \\
     & =\arg\frac{1}{-\alpha}                      \\
     & =2\pi-\arg(-\alpha)                         \\
     & =2\pi-(\arg(-1)+\arg\alpha)                 \\
     & =\pi-\arg\alpha                             \\
     & = \frac{3\pi}{5}
  \end{align*}
  となり，よって$\theta_1=\theta_2$であり題意は示された．

  % $$ \theta_1 = \arg\frac{Z}{\alpha^2} $$
  % $$ = \arg\frac{\alpha^2(\alpha^2+1)}{\alpha^2+\alpha} $$
  % $$ = \arg\frac{\alpha^2(1-\alpha)}{1+\alpha} $$
  % $$ = 2\arg\alpha + \arg(\alpha+2) - \arg(\alpha+1) \quad \dots \textcircled{1} $$
  % $$ \theta_2 = \arg\frac{\alpha^2-1}{Z-1} = \arg\alpha + \arg(\alpha+1) + \arg(\alpha+2) \quad \dots \textcircled{2} $$
  % $\arg\alpha = \frac{2}{5}\pi$, $\arg(\alpha+\bar{\alpha}) = 0$, $\alpha+1 = 2\cos\frac{\pi}{5}e^{i\frac{\pi}{5}}$ から
  % $\arg(\alpha+1) = \frac{\pi}{5}$ だから，\textcircled{1}, \textcircled{2} とあわせて $\theta_1=\theta_2$ 終

  \newpage
\end{multicols}
\end{document}