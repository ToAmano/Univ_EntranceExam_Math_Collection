\documentclass[../../../../main.tex]{subfiles}
% \documentclass[a4paper,10pt]{ltjsarticle}
\usepackage{luatexja}
\usepackage[hiragino-pron]{luatexja-preset}

\usepackage[truedimen,top=25truemm,bottom=20truemm,left=15truemm,right=15truemm]{geometry}
\setlength{\textwidth}{54\zw}
\setlength{\textheight}{73\zw}

\usepackage{amsmath,amssymb,ascmac}
\usepackage{enumerate}
\usepackage{multicol}
\usepackage{cleveref}
\usepackage{framed}
\usepackage{fancyhdr}
\usepackage{latexsym}
\usepackage{tikz}
\usepackage{pgfplots}
 \usetikzlibrary{math}
% \usepackage{indent}
\usepackage{cases}
\usepackage{float}
\allowdisplaybreaks
\pagestyle{fancy}
\lhead{}
\chead{}
\begin{document}
\rhead{京都大学後期$2002$年$2$番}
\setcounter{equation}{0}
\begin{oframed}
    \input{problem.tex}
\end{oframed}
\setlength{\columnseprule}{0.4pt}
\begin{multicols}{2}
    {\bf[解]}

    \begin{figure}[H]
        \centering
        \begin{tikzpicture}
            \begin{axis}[
                    axis lines=middle,
                    xlabel=$x$,
                    ylabel=$y$,
                    xmin=-3, xmax=3,
                    ymin=-2.5, ymax=2.5,
                    xtick={-1,1}, % Mark the ellipse's x-intercepts
                    ytick={-2,2}, % Mark the ellipse's y-intercepts
                    grid=none, % No grid for a cleaner look
                    clip=false, % Allow labels/arrows to go outside
                    % Add coordinate labels for the ellipse intercepts
                    % after end axis/.code={
                    %         \node[below left] at (axis cs:1,0) {$1$};
                    %         \node[below right] at (axis cs:-1,0) {$-1$};
                    %         \node[left] at (axis cs:0,2) {$2$};
                    %         \node[left] at (axis cs:0,-2) {$-2$};
                    %     }
                ]

                % Plot the ellipse: x^2 + y^2/4 = 1  => y = +- 2*sqrt(1-x^2)
                \addplot[domain=-1:1, samples=100, blue, thick] {2*sqrt(1-x^2)};
                \addplot[domain=-1:1, samples=100, blue, thick] {-2*sqrt(1-x^2)};
                \node[above right, blue] at (axis cs:0.7, 1.5) {$C_1$}; % Label for the ellipse

                % Plot the circle: (x-a)^2 + y^2 = b
                % We'll use a=0.5 and b=1.25 for illustration to get 4 intersection points
                \def\myA{0.5} % value for 'a'
                \def\myB{1.25} % value for 'b' (radius^2)
                \addplot[domain=\myA-sqrt(\myB):\myA+sqrt(\myB), samples=100, red, thick] {sqrt(\myB-(x-\myA)^2)};
                \addplot[domain=\myA-sqrt(\myB):\myA+sqrt(\myB), samples=100, red, thick] {-sqrt(\myB-(x-\myA)^2)};
                \node[above left, red] at (axis cs:1.7, 0.7) {$C_2$}; % Label for the circle

                % Add points for the center of the circle for clarity
                \fill[red] (axis cs:\myA,0) circle (1.5pt);
                \node[below, red] at (axis cs:\myA,0) {$(a,0)$};

            \end{axis}
        \end{tikzpicture}
        \caption{楕円と円の様子}
        \label{2002-2:fig:1}
    \end{figure}

    楕円と円を$C_1$, $C_2$とする．
    \begin{align}
         & C_1: \frac{x^2}{1} + \frac{y^2}{4} = 1 \label{2002-2:eq:1} \\
         & C_2: (x-a)^2 + y^2 = b \quad (b>0) \label{2002-2:eq:2}
    \end{align}
    すると$C_1,C_2$は$x$軸に関して対称だから，
    \cref{2002-2:eq:1,2002-2:eq:2}が$(\alpha,\beta)$ $(\beta>0)$を
    交点に持てば, $(\alpha,-\beta)$も交点になる.
    従って, \cref{2002-2:eq:1}を\cref{2002-2:eq:2}に代入して得られる$x$の2次方程式
    \begin{align}
        (x-a)^2 + 4(1-x^2) = b \label{2002-2:eq:3}
    \end{align}
    が $C_1$の$x$の範囲である$-1< x< 1$ に2異解を持てば良い.
    $x=\pm 1$の時は交点が$4$つにならず二つになるから不適である．

    \cref{2002-2:eq:3}を展開して
    \begin{align*}
         & -3x^2 - 2ax + a^2 - 4 - b = 0 \\
        \therefore
         & 3x^2 + 2ax - a^2 + 4 + b = 0
    \end{align*}
    だから，左辺を$f(x)$
    \begin{align*}
        f(x) = 3x^2 + 2ax - a^2 + 4 + b
    \end{align*}
    とおいて，$f(x)=0$の判別式を$D$として
    $f(x)=0$が$-1<x<1$に2異解を持つための条件は以下の通り．

    \begin{align*}
         & \begin{dcases}
               D/4>0                        \\
               \text{端点:} f(1)> 0, f(-1)> 0 \\
               \text{軸:}   -1 < -a/3 < 1
           \end{dcases} \\
        \therefore
         & \begin{dcases}
               a^2 - 3(-a^2-4+b) > 0 \\
               -a^2 + 2a - 1 + b > 0 \\
               -a^2 - 2a - 1 + b > 0 \\
               -3 < a < 3
           \end{dcases}
    \end{align*}

    この領域を図示して\cref{2002-2:fig:2}斜線部(境界含まず)

    \begin{figure}[H]
        \centering
        \begin{tikzpicture}
            \begin{axis}[
                    axis lines=middle,
                    xlabel=$a$,
                    ylabel=$b$,
                    xmin=-3.5, xmax=3.5,
                    ymin=0, ymax=17, % Adjusted ymax to show b=16 clearly
                    xtick={-1,1},
                    ytick={1,4,16}, % Specific y-ticks from the image
                    xticklabels={$-1$,$1$},
                    grid=none,
                    clip=false, % Allow labels to go outside axis limits
                    % Add coordinate labels for clarity if needed, or just let the ticks be
                    % after end axis/.code={
                    %         \node[above right] at (axis cs:3,16) {}; % Top-right intersection
                    %         \node[above left] at (axis cs:-3,16) {}; % Top-left intersection
                    %         \node[below] at (axis cs:0,1) {}; % Bottom intersection
                    %     }
                ]

                % Define paths for each parabola
                % Curve 1: b = 4/3 a^2 + 4
                \addplot[name path=P1, domain=-3.1:3.1, samples=100, thick, dashed, blue] {4/3*x^2+4};
                \node[above right, blue] at (axis cs:2.5,12) {$b = \frac{4}{3}a^2+4$};

                % Curve 2: b = a^2 + 2a + 1  (= (a+1)^2)
                % We'll plot it across the domain but use softclip for filling
                \addplot[name path=P2, domain=-3.1:3.1, samples=100, thick, dashed, red] {x^2+2*x+1};
                \node[left, red] at (axis cs:-2.5,6.5) {$b = a^2+2a+1$};

                % Curve 3: b = a^2 - 2a + 1 (= (a-1)^2)
                % We'll plot it across the domain but use softclip for filling
                \addplot[name path=P3, domain=-3.1:3.1, samples=100, thick, dashed, green!70!black] {x^2-2*x+1};
                \node[right, green!70!black] at (axis cs:2.5,6.5) {$b = a^2-2a+1$};

                % Fill the region
                % The region is between P1 and max(P2, P3)
                % For a < 0, the lower boundary is P2. For a > 0, the lower boundary is P3.
                % So, we fill in two parts.
                \addplot[gray, pattern=north east lines] fill between[of=P1 and P3, soft clip={domain=-3:0}];
                \addplot[gray, pattern=north east lines] fill between[of=P1 and P2, soft clip={domain=0:3}];

                % Optionally, add points for the intersection
                \addplot[only marks, mark=*, mark options={fill=white, scale=1.5}] coordinates {(3,16) (-3,16) (0,1)};
                % \fill[white] (axis cs:3,16) circle (2pt);
                % \fill[white] (axis cs:-3,16) circle (2pt);
                % \fill[white] (axis cs:0,1) circle (2pt);

            \end{axis}
        \end{tikzpicture}
        \caption{楕円と円が$4$つの交点を持つための$a,b$の範囲．境界は含まない．}
        \label{2002-2:fig:2}
    \end{figure}

    $\cdots$(答)
    {\bf[解説]}
    % TODO :: 二次関数の軸の様子を簡単なグラフにしたい

    \newpage
\end{multicols}
\end{document}