\documentclass[tikz,border=2pt]{standalone}
\usepackage{amsmath,amssymb,amsfonts,amsthm}
\usepackage{tikz,pgfplots}
\usepackage{tikz-3dplot}
\usetikzlibrary{arrows.meta,calc,intersections,patterns,patterns.meta,angles,quotes,through,positioning,decorations.pathmorphing,decorations.markings}
\usepgfplotslibrary{fillbetween,colormaps}
\pgfplotsset{compat=1.18}

\def\thA{50}
\def\thB{\thA + 90}
\def\thA{60}
\def\thB{\thA + 90}
\def\thA{45}
\def\thB{\thA + 90}
\pgfmathsetmacro{\xPtwo}{1 - sqrt(3)/2}
\pgfmathsetmacro{\yPtwo}{0.5}

\begin{document}
\begin{tikzpicture}
      \begin{axis}[
          axis lines=middle,
          xlabel=$x$,
          ylabel=$y$,
          xmin=-0.1, xmax=1.2,
          ymin=-0.1, ymax=1.2,
          xtick={\empty},
          ytick={1},
          axis equal,
          xlabel style={anchor=north},
          ylabel style={anchor=east},
          enlargelimits=false,
        ]

        \pgfmathsetmacro{\xPtwo}{1 - sqrt(3)/2}
        \pgfmathsetmacro{\yPtwo}{0.5}
        \coordinate (P2) at (\xPtwo, \yPtwo);


        \addplot[domain=90:180, samples=50, dashed] ({1+cos(x)}, {sin(x)});
        \addplot[domain=150:180, samples=50, thick, name path=circle] ({1+cos(x)}, {sin(x)});

        \addplot[domain=45:60, samples=50, thick, name path=curve]
        ({sin(x)^2 - cos(x)^2 - (sin(x)-cos(x))}, {sin(x)+cos(x) - 2*sin(x)*cos(x)});

        \addplot[pattern=north west lines] fill between [of=circle and curve];

        \draw[dashed] (0,0) rectangle (1,1);
        \coordinate (O) at (0,0);
        \coordinate (C) at (1,0);
        \coordinate (P2_proj) at (\xPtwo, 0);
        \coordinate (F) at (0,{sqrt(2)-1});
        \coordinate (G) at ({1-sqrt(3)/2},{1/2});
        \coordinate (H) at ({1-sqrt(3)/2},0);
        \coordinate (I) at (1,0);

        \draw[dashed] (C) -- (P2) -- (P2_proj);

        \fill[] (P2) circle (1.5pt);
        \fill[] (F) circle (1.5pt);
        \fill[] (O) circle (1.5pt);
        \node[below] at (O) {O};
        \node[left] at (F) {F};
        \node[above] at (G) {G};
        \node[below] at (H) {H};
        \node[below] at (I) {I};

        \draw[->, thin] (0.5, 0.8) node[above right] {$\theta=\pi/4$} to[out=200, in=50] (0.05, {sqrt(2)-1+0.05});
        \draw[->, thin] (0.5, 0.3) node[right] {$\theta=\pi/3$} to[out=180, in=0] (P2);

        \pic[draw=black, angle eccentricity=1.6,"$\pi/6$", angle radius=10mm, font=\small]{angle = P2--C--O};

      \end{axis}
    \end{tikzpicture}
\end{document}
